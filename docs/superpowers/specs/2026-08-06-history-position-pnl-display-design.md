# 历史仓位盈亏显示与排序设计

## 目标

在量化交易工作台的“历史仓位”表格中，将“已实现收益”按盈亏状态着色，并支持按数值排序。

## 方案

- 初始化历史仓位表格时启用表头排序。
- “已实现收益”单元格使用原始 `realized_pnl` 数值写入 `Qt.ItemDataRole.UserRole`，避免按格式化后的文本排序。
- 正数使用现有盈利绿色，负数使用现有亏损红色，零值和缺失值保持默认前景色。
- 刷新、筛选和数据重绘时重新设置排序数据与颜色。

## 验证

增加 Qt 回归测试，覆盖表格排序开关、正负零颜色以及数值排序数据。
