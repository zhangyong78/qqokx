# 账户实盘复盘与回测分析规则

版本：`v1.0`

更新日期：`2026-09-09`

状态：`现行正式规则`

适用范围：OKX 账户按 API 配置名隔离的实盘复盘、指定币对的历史回测、实盘与回测差异分析，以及中文报告输出。

本文件与 [回测规则文件.MD](D:\qqokx\回测规则文件.MD) 一起使用。本文件优先规定“账户如何复盘、实盘和回测如何对照”；`回测规则文件.MD` 规定常规研究回测的默认资金与执行口径。

---

## 1. 目标与不可变原则

一次分析必须同时回答四件事：

1. 指定账户在指定时间段真实赚亏多少，资金去向是否能对账。
2. 盈亏来自哪些币对、方向、交易、持仓时间和退出方式。
3. 预先存在的策略参数在同一时间段的回测结果如何，实盘与回测差异来自哪里。
4. 哪些判断已被数据支持，哪些只能列为待验证假设。

必须遵守：

- 账户、环境、API 配置名必须隔离。不同 API 的成交、资金、仓位、净盈亏绝不混算。
- 全程只读账户数据；不得下单、撤单、改参数、启停策略或写入账户运行状态。
- 必须固定分析区间、时区、币对列表、回测代码版本、参数包版本和成本假设。
- 实盘数据、回测数据、人工判断必须分栏呈现，不能用叙述把三者混为事实。
- 先审计数据，再计算统计，最后写结论。任一关键审计失败时，只能出“数据异常/待核对报告”，不能给策略优劣结论。
- 不得用目标月数据调参后，再把同一个月称为独立验证或“最佳参数”。

---

## 2. 每次任务必须先登记的分析卡

报告开头必须列出以下字段；缺一项则停止自动结论，向用户追问或标记缺失。

| 字段 | 必填规则 |
| --- | --- |
| 账户/API 配置名 | 精确名称，例如 `ReapAI`；不得用“当前账户”代替。 |
| 环境 | `live` 或 `demo`。 |
| 时间区间 | `[开始时间, 结束时间)`，统一毫秒时间戳；报告显示北京时间。 |
| 标的 | 明确列出币对，例如 BTC、ETH、SOL、DOGE 的 `USDT-SWAP`。 |
| 实盘口径 | 已平仓生命周期、账单现金变动、月末未平仓浮盈三者分开。 |
| 参数来源 | 参数包文件、创建时间、SHA-256；必须早于验证月才可称“预先参数”。 |
| 回测口径 | 原执行复现、统一下一根开盘，或两者都做。 |
| 风险口径 | 100U 固定风险、参数包参考风险，或用户明确指定的其他值。 |
| 成本口径 | Maker/Taker 费率、滑点、资金费处理方式。 |
| 代码版本 | 关键策略代码和模型文件的 SHA-256。 |

默认时区：北京时间（UTC+8）。所有原始时间保留 UTC 毫秒值，展示层才换算为北京时间。

---

## 3. 实盘账户数据采集规则

### 3.1 必要原始数据

对每一个指定 API、环境、币对，必须保留以下原始快照：

- 完整成交 `fills`；
- 账户账单 `bills`；
- 已平仓仓位/仓位历史 `positions`；
- 合约信息 `instrument`，尤其是 `ctVal`、`lotSz`、`minSz`；
- 指定区间 K 线；
- 历史资金费率；
- 可用时的策略会话历史、策略账本和参数包。

原始 JSON 不得覆盖；报告目录中必须保存下载时间、API 配置名、环境、币对、时间范围和文件哈希。

### 3.2 生命周期重建

实盘交易的正式统计单位是“完整平仓生命周期”，不是单条成交。

- 同一方向连续开仓、减仓、平仓，要按 OKX 仓位/成交关联重建为一个生命周期。
- 每个生命周期至少包含：币对、方向、首笔开仓时间、最后平仓时间、平均开仓价、平均平仓价、实际张数、毛损益、手续费、资金费、净损益、持仓时长。
- 只有在分析区间内平仓的生命周期进入“当月已实现交易统计”。
- 跨月开仓、当月平仓的交易必须保留，并单独标注“跨月开仓”。
- 月末未平仓不能混入已实现月度净盈亏；须单独列出数量、均价、最后已确认 K 线估算浮盈及其非标记价性质。

### 3.3 账户对账（必须通过）

必须至少做以下核对：

1. 成交与账单关联：交易相关 `billId` 与成交记录一一核对，无重复、无遗漏。
2. 生命周期核对：每笔生命周期的成交毛损益、手续费和资金费，与其分配到的成交/账单分项相等；允许的数值残差应写入报告。
3. 现金对账：

   `期末现金余额 - 期初现金余额 = 交易相关账单净变动 + 非交易项目（如自动赚币） + 其他已列明项目`

4. 合约单位核对：金额计算必须使用 `ctVal × ctMult × 张数`；不能把“张数”直接当成币数量。
5. K 线完整性：每个币对指定周期内无缺口、无重复、已确认 K 线数量符合区间长度。

若任何一项失败，报告首页必须标红“审计未通过”，并列出受影响的原始文件和残差；停止收益归因和参数优劣判断。

### 3.4 实盘统计的固定输出

每次必须输出：

- 总笔数、胜/负笔数、胜率、毛损益、手续费、资金费、净损益、PF、平均盈利、平均亏损、盈亏比；
- 按币对、方向、币对×方向拆分；
- 按周或等长时段拆分；
- 已实现累计净值、最大已实现回撤、最长连亏；
- 最大盈利/亏损交易及前 N 笔大额交易明细；
- 跨月平仓交易、月末未平仓、异常或未匹配交易；
- 账户现金对账表。

已实现回撤只能表述为“按平仓顺序计算的已实现回撤”。没有逐时点权益和标记价快照时，禁止把它称为真实账户最大权益回撤。

---

## 4. 实盘复盘判断规则

### 4.1 必须先回答的顺序

1. 数据是否完整、对账是否通过。
2. 月度总结果是否集中于少数大盈利交易。
3. 哪些币对、方向和时段贡献盈利或亏损。
4. 大盈利交易是否按既定入场、保护和持仓规则运行。
5. 大亏交易的实际张数、初始止损距离、费用和执行路径是否可核实。
6. 实盘与回测是否在同类币对、方向、时间段和风险口径下可比。

### 4.2 禁止的推断

- 单月盈利不等于策略稳定、可扩仓或参数最优。
- 总笔数相同不等于实盘和回测逐笔一致。
- 没有历史初始止损和会话快照时，不得断言某笔交易“风控失效”。
- 没有逐笔信号日志时，不得仅因时间接近就宣称已证明同一策略信号。
- 不能把现金余额当作账户总权益；现金不含未实现盈亏。
- 空头或多头的单月亏损不能直接归因于人工操作、滑点或策略本身，必须先看同口径回测和执行差异。

### 4.3 结论强度标签

每个关键结论必须带一个标签：

| 标签 | 使用条件 |
| --- | --- |
| 已核实 | 可由原始数据或脚本审计直接重算。 |
| 高可信推断 | 多项数据一致，但缺少逐笔信号或会话证据。 |
| 待验证假设 | 有现象但不能排除其他解释。 |
| 不可判断 | 缺数据或审计失败。 |

---

## 5. 回测规则

### 5.1 参数与样本隔离

- 回测验证月前，必须锁定参数包和代码版本。
- 参数创建日期晚于或等于验证期开始日时，该验证只能称“同样本重放”，不能称样本外验证。
- 需要优化时，使用验证月之前的训练区间选参；验证月和后续月份只用于留出检验。
- 报告必须写明参数包来源、创建时间、策略条数、币对、方向、完整动态保护规则和参考风险。

### 5.2 K 线与预热

- 只使用已确认 K 线。
- 策略指标必须有足够预热数据；至少比较两个不同预热起点，验证目标月交易时间和损益一致。
- 指定区间为按平仓时间归属；月末仓位默认保留，不强制平仓。
- 统一下一根开盘口径必须做前缀稳定性检查：截断未来数据后，截断点之前的交易不能变化。

### 5.3 两套执行口径必须分开报告

#### A. 原执行复现

用途：近似复现当前生产策略的历史执行方式，用于与实盘执行差异对照。

- 保留生产策略原有信号、限价等待、当根/下一根成交、信号退出和盘中 OHLC 路径；
- 不能把它写成“统一下一根开盘”或“纯策略表现”；
- 若生产代码的成交逻辑存在不对称成本或限价假设，必须显式写出。

#### B. 统一下一根开盘

用途：规则研究和可审计的标准执行对照。

- 信号只在当前已收 K 线产生；
- 信号开仓与信号型退出均在下一根 K 线开盘执行；
- 入场当根触及止损的处理、盘中止损止盈路径必须固定并记录；
- 每笔必须校验：`signal_time + bar_interval = entry_time`，以及滑点后的成交价符合下一根开盘规则；
- 若交易笔数因执行假设变化而改变，必须说明这是执行口径差异，不是参数优化收益。

两套口径的净损益、笔数、胜率、回撤不能互相拼接或相减后得出结论。

### 5.4 默认风险与成本口径

除非用户明确指定，常规标准回测遵循 [回测规则文件.MD](D:\qqokx\回测规则文件.MD)：

- 初始参考资金 10000U；
- 固定风险 100U/笔；
- 非复利；
- 不默认增加组合资金池、总仓位、方向或单币种暴露约束；
- 手续费、滑点必须计入；
- 多空、币对、策略拆分必须输出。

账户复盘允许额外运行“参数包参考风险”版本，目的是接近参数包的风险级别。它不能替代 100U 标准版，二者必须并排显示并明确标签。

资金费须使用历史费率；若没有逐结算标记价和精确盘中持仓时刻，只能写为“资金费估算”，不能宣称精确复刻实盘。

### 5.5 合约单位与定仓

- 风险定仓必须先把币数量与合约张数转换正确，再按 `lotSz`、`minSz` 取整。
- 每个币对的 `ctVal`、`ctMult`、最小张数、最小数量须保存到审计输出。
- 未校准的旧引擎结果可以保留作历史审计对照，但不能优先用来解释实盘差异。

---

## 6. 实盘与回测差异分析规则

### 6.1 可比前提

只有同时满足以下条件，才允许给出“实盘 - 回测”总差额：

- 相同币对、方向、验证区间；
- 明确相同或可换算的风险口径；
- 相同的费用、资金费和合约单位定义，或差异已单独列出；
- 回测参数与代码版本已记录；
- 实盘已通过生命周期和账单对账。

### 6.2 匹配方法

逐笔匹配只能叫“候选匹配”，除非有相同客户端订单号、策略会话 ID 或信号 ID。

- 候选匹配可使用币对、方向、入场时间窗口、离场时间窗口；
- 必须输出已匹配、仅实盘、仅回测三张明细；
- 必须披露匹配窗口和匹配数；
- 时间接近不证明策略 ID、信号和参数完全相同。

### 6.3 差额归因

只允许按以下结构汇总，不得把全部差额笼统称为滑点：

| 差额项 | 可包含内容 |
| --- | --- |
| 价格/交易序列/仓位差异 | 信号时点、限价是否成交、开平仓价格、减仓、仓位大小、止损路径。 |
| 手续费差异 | 实际成交费率与回测假设的差。 |
| 资金费差异 | 实际账单与历史费率估算的差。 |
| 未匹配交易 | 仅实盘或仅回测的交易，单独列示，不强行归因。 |

---

## 7. 报告与审计产物

每次生成独立目录，建议：`reports/<api>_<YYYYMM>_full/`。

目录必须至少包含：

```text
raw/ 或 *_raw.json                 原始仓位、成交、账单、K线、合约、资金费
parameter_bundle.json              锁定参数包
live_trades.csv                    完整已平仓生命周期
live_fills.csv / live_bills.csv    原始拆单与账单导出
live_summary.json                  实盘汇总
backtest_*.csv                     每个回测口径的逐笔结果
summary_*.json                     每个回测口径的汇总
open_positions_*.json              月末未平仓回测头寸
execution_adapter_audit.json       执行口径变更审计
trade_match_candidates.csv         实盘/回测候选匹配
unmatched_live_trades.csv          仅实盘交易
unmatched_replay_trades.csv        仅回测交易
verification.json                  审计检查与残差
run_manifest.json                  运行配置、文件哈希、代码哈希
report.md / report.html            中文报告
```

报告正文固定顺序：

1. 分析卡与审计状态；
2. 一页结论与结论强度标签；
3. 实盘总览、多空/币对拆分、回撤与大单；
4. 账单现金对账、跨月和月末未平仓；
5. 参数快照与样本隔离说明；
6. 回测总览、执行口径、成本和合约单位审计；
7. 实盘与回测差异、候选匹配和未匹配交易；
8. 数据局限、禁止推断事项、后续验证清单；
9. 附录：逐笔明细和文件清单。

报告中不得出现 API Key、Secret Key、Passphrase、完整账户标识或任何敏感配置。

---

## 8. ReapAI 2026 年 8 月的基准实例

本规则的首个完整实例为 [ReapAI 2026年8月完整复盘与最佳参数回测](D:\qqokx\reports\reapai_august_2026_full\report.md)。该实例的固定事实包括：

- 账户：`ReapAI/live`；币对：BTC、ETH、SOL、DOGE 的 USDT 永续；
- 区间：2026-08-01 至 2026-09-01（北京时间展示）；
- 实盘：578 条成交、100 笔完整平仓生命周期、1524 条账单；已平仓净损益 `+114.95U`；
- 账单交易相关净变动 `+115.37U`，自动赚币 `+4.42U`，现金余额由 `2554.13U` 变为 `2673.92U`；
- 回测参数来自验证期之前保存的 8 条策略参数包，未用 8 月数据搜索参数；
- 对照包含“合约单位校准/原执行”和“统一下一根开盘”两种口径。

这些数字仅用于验证本规则是否被正确执行，不应自动迁移为其他账户、月份或策略的结论。

---

## 9. 给模型的执行协议

### 9.1 模型职责边界

模型的职责是：检查清单、阅读结构化产物、发现矛盾、按模板写中文报告。

模型不得：

- 从自然语言猜测未导出的数字；
- 计算后覆盖脚本生成的统计；
- 在审计失败时补写“看起来合理”的结论；
- 修改实盘账户、策略参数或交易状态；
- 将本月回测优化称为样本外验证。

数值真源优先级固定为：

`原始 JSON/CSV → verification.json 与 run_manifest.json → 脚本汇总 JSON → 报告正文 → 模型叙述`。

如上下层数字冲突，模型必须停止结论，列出冲突文件、字段和值；不能自行挑选一个数字。

### 9.2 轻量模型可执行的标准任务

在下列全部条件满足时，可用 `gpt-5.6-luna` 的 `low` 推理档执行月度常规报告：

- 沿用已验证脚本和本规则，未改策略、参数结构、成本口径或数据源；
- `verification.json` 的所有硬性检查通过；
- 原始数据齐全，K 线无缺口，生命周期与现金对账残差在阈值内；
- 仅复用已锁定参数包，不做参数搜索；
- 输出只限于规则中规定的表、结论标签和异常清单。

轻量模型的提示必须包含：账户名、时间区间、币对、报告目录、规则文件路径，并要求“仅引用文件内已验证数字；所有不确定结论标为待验证”。

### 9.3 必须升级到 Terra 高推理的情形

出现任一项时，使用 `gpt-5.6-terra` 的 `high` 或更高档：

- 首次接入新账户、新 API、币对或时间周期；
- 对账残差、缺失 K 线、未匹配交易或合约单位出现异常；
- 修改策略代码、参数包、成交口径、滑点、费用或资金费逻辑；
- 需要解释实盘与回测显著差额，或做逐笔归因；
- 需要参数优化、样本外设计、组合约束、风险规则改变；
- 轻量模型报告中的数字与汇总文件不一致。

### 9.4 一致性标准

`gpt-5.6-luna/low` 与 `gpt-5.6-terra/high` 的自然语言和复盘重点不保证完全相同，也不应要求逐字相同。

在相同输入文件、相同规则版本、相同脚本输出下，合格的一致性是：

- 所有表格数字、审计状态、结论强度标签必须相同；
- 所有“已核实”结论必须相同；
- 后续建议可以不同，但不能与硬数据或禁止推断规则冲突；
- 任何一方发现异常，都以升级后人工/高推理复核为准。

因此，节省 token 的关键不是让 Luna 做更多计算，而是把计算、核对和文件格式交给脚本，把模型限制为可验证的报告解释。

---

## 10. 标准执行提示词

```text
按《账户实盘复盘与回测分析规则.md》执行账户复盘。

账户/API：<API名称>；环境：<live/demo>
区间：[<开始>, <结束>)；时区：北京时间展示
币对：<列表>
报告目录：<绝对路径>

先检查 run_manifest.json 和 verification.json。任何硬性检查失败时，停止策略优劣结论，只输出“审计异常报告”。
只引用目录内 JSON/CSV 中已验证的数字；不得补算、猜测、改动账户或策略。
分别输出实盘已实现结果、账单现金对账、月末未平仓、原执行复现回测、统一下一根开盘回测；不得混用两种回测口径。
每个结论标注：已核实 / 高可信推断 / 待验证假设 / 不可判断。
```

---

## 11. 一句话规则

以后说“按账户规则复盘并回测”，默认含义是：按本文件锁定账户、时间、参数、执行和成本口径；先完成实盘生命周期与账单对账，再分别运行原执行复现和统一下一根开盘回测，输出可审计中文报告，并把未证实判断明确留在“待验证”层。
