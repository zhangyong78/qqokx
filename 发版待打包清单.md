# 发版待打包清单
## 已收录

### v0.5.16 | 2026-05-06 | 收口当前工作区改动
### v0.5.16 | 2026-05-06 | 收口当前工作区改动
- 收口当前工作区改动
- 相关文件：scripts/release_one_click.ps1
- 本次按 $BumpLevel 级别处理，版本已递进到 0.5.16。
# 发版待打包清单
## 已收录

### v0.5.15 | 2026-05-06 | 更新 README 中文更新日志与版本说明、同步包内版本号、调整回测逻辑与口径、完善持久化与配置存储、整理主界面壳层与布局、同步项目版本号、新增分析或报表脚本
### v0.5.15 | 2026-05-06 | 更新 README 中文更新日志与版本说明、同步包内版本号、调整回测逻辑与口径、完善持久化与配置存储、整理主界面壳层与布局、同步项目版本号、新增分析或报表脚本
- 更新 README 中文更新日志与版本说明
- 同步包内版本号
- 调整回测逻辑与口径
- 完善持久化与配置存储
- 整理主界面壳层与布局
- 同步项目版本号
- 新增分析或报表脚本
- 相关文件：\345\217\221\347\211\210\345\276\205\346\211\223\345\214\205\346\270\205\345\215\225.md、\345\233\236\346\265\213\350\246\201\346\261\202\346\240\207\345\207\206_v1.json、\345\233\236\346\265\213\350\246\201\346\261\202\346\240\207\345\207\206_v1.md、\350\275\257\344\273\266\345\274\200\345\217\221\346\214\207\345\215\227.md、okx_quant/__init__.py、okx_quant/backtest.py、okx_quant/journal.py、okx_quant/journal_ui.py、okx_quant/persistence.py、okx_quant/ui_shell.py、pyproject.toml、README.md、scripts/generate_ema_breakout_directional_summary.py、scripts/generate_multi_coin_ema_breakout_summary.py、scripts/release_one_click.bat、scripts/release_one_click.ps1、scripts/run_ema_breakout_fixed_group_matrix.py、scripts/run_ema_breakout_group_matrix.py、scripts/run_ema_breakout_single_metric.py、tests/test_journal.py
- 本次按 $BumpLevel 级别处理，版本已递进到 0.5.15。
# 发版待打包清单
## 已收录

### v0.5.14 | 2026-05-06 | 更新 README 中文更新日志与版本说明、同步包内版本号、调整回测逻辑与口径、完善持久化与配置存储、整理主界面壳层与布局、同步项目版本号、新增分析或报表脚本
### v0.5.14 | 2026-05-06 | 更新 README 中文更新日志与版本说明、同步包内版本号、调整回测逻辑与口径、完善持久化与配置存储、整理主界面壳层与布局、同步项目版本号、新增分析或报表脚本
- 更新 README 中文更新日志与版本说明
- 同步包内版本号
- 调整回测逻辑与口径
- 完善持久化与配置存储
- 整理主界面壳层与布局
- 同步项目版本号
- 新增分析或报表脚本
- 相关文件：\345\217\221\347\211\210\345\276\205\346\211\223\345\214\205\346\270\205\345\215\225.md、\345\233\236\346\265\213\350\246\201\346\261\202\346\240\207\345\207\206_v1.json、\345\233\236\346\265\213\350\246\201\346\261\202\346\240\207\345\207\206_v1.md、\350\275\257\344\273\266\345\274\200\345\217\221\346\214\207\345\215\227.md、okx_quant/__init__.py、okx_quant/backtest.py、okx_quant/journal.py、okx_quant/journal_ui.py、okx_quant/persistence.py、okx_quant/ui_shell.py、pyproject.toml、README.md、scripts/generate_ema_breakout_directional_summary.py、scripts/generate_multi_coin_ema_breakout_summary.py、scripts/release_one_click.bat、scripts/release_one_click.ps1、scripts/run_ema_breakout_fixed_group_matrix.py、scripts/run_ema_breakout_group_matrix.py、scripts/run_ema_breakout_single_metric.py、tests/test_journal.py
- 本次按 $BumpLevel 级别处理，版本已递进到 0.5.14。
# 发版待打包清单
## 已收录

### v0.5.13 | 2026-05-06 | 调整回测逻辑与口径、完善持久化与配置存储、整理主界面壳层与布局、新增分析或报表脚本
### v0.5.13 | 2026-05-06 | 调整回测逻辑与口径、完善持久化与配置存储、整理主界面壳层与布局、新增分析或报表脚本
- 调整回测逻辑与口径
- 完善持久化与配置存储
- 整理主界面壳层与布局
- 新增分析或报表脚本
- 相关文件：\345\233\236\346\265\213\350\246\201\346\261\202\346\240\207\345\207\206_v1.json、\345\233\236\346\265\213\350\246\201\346\261\202\346\240\207\345\207\206_v1.md、okx_quant/backtest.py、okx_quant/journal.py、okx_quant/journal_ui.py、okx_quant/persistence.py、okx_quant/ui_shell.py、scripts/generate_ema_breakout_directional_summary.py、scripts/generate_multi_coin_ema_breakout_summary.py、scripts/release_one_click.bat、scripts/release_one_click.ps1、scripts/run_ema_breakout_fixed_group_matrix.py、scripts/run_ema_breakout_group_matrix.py、scripts/run_ema_breakout_single_metric.py、tests/test_journal.py
- 本次按 $BumpLevel 级别处理，版本已递进到 0.5.13。
# 发版待打包清单
## 已收录

### v0.5.12 | 2026-05-05 | BTC 分析对齐与 EMA 方向报表
- 已补充 `btc_analysis_alignment_checklist_v1.md`，用于记录 `BTC 自动技术分析系统` 的需求边界、当前进度、待领导确认项和建议方案。
- 已新增 `scripts/run_ema_breakout_directional_report.py`，用于批量导出 EMA 突破方向相关的分析报表。
- 已继续整理 `okx_quant/persistence.py`、`okx_quant/ui_positions.py`、`okx_quant/ui_shell.py`、`tests/test_persistence.py`、`tests/test_ui.py`，将历史仓位日期筛选与视图偏好写入统一存储。
- 已将当前版本同步到 `v0.5.12`，并继续沿用 `v0.5.x` 版本线。
- 本次按新增分析对齐与报表能力处理，版本级别按 `minor` 递增到 `v0.5.12`。
### v0.5.05 | 2026-05-05 | BTC 分析对齐与多模块联动
- 已补充 `btc_analysis_alignment_checklist_v1.md`，用于记录 `BTC 自动技术分析系统` 的需求边界、当前进度、待领导确认项和建议方案。
- 已继续整理 `okx_quant/engine.py`、`okx_quant/signal_monitor_ui.py`、`okx_quant/strategy_live_chart.py`、`okx_quant/trader_desk.py`、`okx_quant/trader_desk_ui.py`、`okx_quant/ui.py` 及其测试，形成当前工作区的联动收口。
- 已同步整理分析清单、报表脚本与持仓备注/账本链路，方便后续按领导确认项继续拆分。
- 本次按新增分析对齐与多模块联动处理，版本级别按 `minor` 递增到 `v0.5.05`。
### v0.5.05 | 2026-04-30 | 交易员同波防重复开仓收口（待总控发版）
- 已修复交易员 `同一波重复开单` 问题：在 `D:/qqokx/okx_quant/ui.py` 接入交易员级 `wave_lock_signal` 波段锁；当本轮信号满足并完成开仓后，同方向同一波内会锁定补位，不再继续开第二单，必须等信号失效/反向后才解除锁并允许下一单。
- 已补齐锁状态流转：开仓同步时写入波段锁、交易员补 watcher 前先检查锁、信号失效后自动解锁；同时保留“首次启动清空旧锁”的安全行为，避免历史锁误伤新启动。
- 已新增显式运行日志：当波段锁生效跳过补位时，会记录“波段锁生效，跳过补位 | 锁方向=... | 等待当前波失效后再开新单”，便于你在实盘日志中直接核验行为。
- 已补充回归测试并通过：`tests/test_ui.py` 新增 `TraderWaveLockTest`，并完成交易员相关单测验证（`tests.test_ui.StrategyTradeTrackingTest`、`tests.test_ui.TraderWaveLockTest`、`tests.test_trader_desk`）。
- 版本建议：`patch`（建议进入 `v0.5.05`）；备注：本条为功能线程登记，等待总控线程按 `123` 统一收口发版。

### v0.5.04 | 2026-04-28 | BTC 分析对齐与多模块联动
- 已补充 `btc_analysis_alignment_checklist_v1.md`，用于记录 `BTC 自动技术分析系统` 的需求边界、当前进度、待领导确认项和建议方案。
- 已继续整理 `okx_quant/engine.py`、`okx_quant/signal_monitor_ui.py`、`okx_quant/strategy_live_chart.py`、`okx_quant/trader_desk.py`、`okx_quant/trader_desk_ui.py`、`okx_quant/ui.py` 及其测试，形成当前工作区的联动收口。
- 已将当前版本同步到 `v0.5.04`，并继续沿用 `v0.5.x` 版本线。
- 本次按新增分析对齐与多模块联动处理，版本级别按 `minor` 递增到 `v0.5.04`。
### v0.5.03 | 2026-04-28 | 邮件 API 上下文补齐
- 已补齐成交通知、异常通知、波动率监控、测试邮件与持仓保护邮件的 API 上下文，避免部分邮件只显示业务内容却没有 API 标识。
- 已将 EmailNotifier 的调用方统一收口到带上下文的发送方式，并同步更新相关测试，确保 API配置 在邮件正文和主题中可追溯。
- 本次按邮件修复处理，版本级别按 patch 递增到 0.5.03。

### v0.5.02 | 2026-04-28 | 策略参数抽象与单根 K 线形态分析
- 已新增 `okx_quant/strategy_parameters.py`，把策略参数定义、固定值与可编辑范围抽成统一配置，便于主界面、回测和观察页共享同一套参数口径。
- 已新增 `okx_quant/candle_patterns.py`，支持对单根 K 线做形态分析与历史回放，并补充 `tests/test_candle_patterns.py` 的回归测试。
- 已在 `okx_quant/ui.py`、`okx_quant/backtest_ui.py`、`okx_quant/signal_monitor_ui.py`、`okx_quant/notifications.py`、`okx_quant/persistence.py`、`okx_quant/trader_desk_ui.py` 等模块接入相关参数展示与联动。
- 本次按新增独立功能处理，版本级别按 `minor` 递增到 `v0.5.02`。

### v0.5.01 | 2026-04-26 | 版本线切换与方向模式安全收口
- 已将当前版本线切换到 `v0.5.01`，同步更新 `pyproject.toml`、`okx_quant/__init__.py`、`README.md` 和 `软件开发指南.md`。
- 已收口下单方向模式安全规则：只有 `交易并下单 + 按交易标的价格（本地）` 时才允许固定买入/固定卖出，其他模式会自动锁定为跟随信号。
- 已保留并同步 `okx_quant/ui.py`、`okx_quant/engine.py`、`tests/test_strategy_engine.py`、`tests/test_ui.py` 的回归与说明。
- 本次按版本线切换处理，版本号从 `v0.4.x` 升级到 `v0.5.01`。

### v0.4.16 | 2026-04-26 | 文档协作与发版规则收口
- 已继续整理 `README.md`、`软件开发指南.md`、`发版协作约定.md`、`线程工作流模板.md`，把说明文字统一为中文，并进一步明确 `123` 的总控发版流程。
- 已把版本号同步到 `v0.4.16`，避免文档、源码版本、更新日志与打包产物继续脱节。
- 已在 `okx_quant/ui.py` 新增普通量化策略总账本窗口，支持按 `API / 交易员 / 策略 / 标的 / 周期 / 方向 / 状态` 组合筛选，并同步补充 `tests/test_ui.py` 的回归断言。

### v0.4.06 | 2026-04-23 | 运行中策略参数导入导出
- 已为 `D:/qqokx/okx_quant/ui.py` 新增运行中策略参数导入导出：运行中策略区域支持 `导出选中参数`、`导入策略参数`，工具菜单也新增对应入口。
- 已补上可复用的策略模板载荷链路：通过 `StrategyTemplateRecord`、`_build_strategy_template_payload(...)`、`_strategy_template_record_from_payload(...)`，可把运行中策略的 `config_snapshot` 导出后再导入复用。
- 导入时已处理 API 差异：模板不包含 API 密钥；若本机存在同名 API 配置则自动切换，否则保留当前 API，并在提示中说明回退原因。
- 启动时若本机存在 `moni` API，则默认优先选中它，方便你做默认测试/监控环境。
- 验证：`python -m py_compile D:/qqokx/okx_quant/ui.py D:/qqokx/tests/test_ui.py`、`python -m unittest D:/qqokx/tests/test_ui.py`（43 项通过）。

### v0.4.05 | 2026-04-23 | API 配置名与断线容错
- 已为交易类邮件补充 API 配置名展示，方便直接从邮件判断是哪一个环境触发。
- 已将主策略引擎和增强策略引擎的邮件上下文透传 `credentials.profile_name`，让成交、异常与 `signal_only` 邮件都能带上准确的 API 标识。
- 已把 `Remote end closed connection without response`、`RemoteDisconnected` 等瞬时断线统一纳入容错链路，覆盖主引擎、OKX 客户端、持仓保护、智能下单和主界面。
- 已补充相关回归测试，并通过 `python -m unittest tests.test_notifications tests.test_strategy_engine tests.test_okx_client_orders tests.test_position_protection tests.test_smart_order tests.test_ui`（145 项通过）。

### v0.4.04 | 2026-04-23 | 策略交易账本、实时浮盈亏与多策略合计
- 已为 `D:\qqokx\okx_quant\persistence.py` 新增策略交易账本持久化文件 `D:\qqokx_data\state\strategy_trade_ledger.json`，用于按每轮交易保存开/平仓时间、均价、数量、手续费、资金费、毛盈亏、净盈亏与结束原因。
- 已为 `D:\qqokx\okx_quant\okx_client.py` 补充账户账单历史读取与 `OkxAccountBillItem`，当前结束归因会同时结合历史委托、历史成交、仓位历史和账户账单，补齐资金费与更完整的交易结算信息。
- 已在 `D:\qqokx\okx_quant\ui.py` 为实盘策略会话新增本轮交易运行态跟踪、结束归因日志、`净盈亏 / 实时浮盈亏` 展示与“多策略合计”摘要；运行中策略和历史策略详情现在都能直接查看交易次数、胜率、手续费、资金费、净盈亏与最近结论。
- 已补强 `D:\qqokx\okx_quant\backtest_ui.py` 与 `D:\qqokx\okx_quant\okx_client.py` 的全量历史同步体验：下载时会显示页级进度、累计根数与最早时间，并按页数间隔自动写回本地缓存 checkpoint。
- 已将剩余正式回测/分析脚本统一切到共享数据目录路径入口：`D:\qqokx\scripts\generate_dynamic_fee_offset_report.py`、`D:\qqokx\scripts\generate_dynamic_two_r_break_even_report.py`、`D:\qqokx\scripts\generate_ema_dynamic_reports.py`、`D:\qqokx\scripts\generate_enhanced_seed_lab_realdata_report.py`、`D:\qqokx\scripts\generate_enhanced_seed_lab_smoke_report.py`、`D:\qqokx\scripts\generate_slot_handoff_strategy_pool_report.py` 不再继续写回旧 `reports/...` 路径。
- 已完成本轮验证：`python -m py_compile okx_quant\backtest_ui.py okx_quant\okx_client.py okx_quant\persistence.py okx_quant\ui.py scripts\generate_dynamic_fee_offset_report.py scripts\generate_dynamic_two_r_break_even_report.py scripts\generate_ema_dynamic_reports.py scripts\generate_enhanced_seed_lab_realdata_report.py scripts\generate_enhanced_seed_lab_smoke_report.py scripts\generate_slot_handoff_strategy_pool_report.py tests\test_candle_cache.py tests\test_okx_client_orders.py tests\test_persistence.py tests\test_ui.py`、`python -m unittest tests.test_candle_cache tests.test_okx_client_orders tests.test_persistence tests.test_ui`（`56` 项测试通过）。
### v0.4.02 | 2026-04-22 | 回测全量历史、批量历史同步与缓存补强
- 已为 `D:\qqokx\okx_quant\backtest_ui.py` 新增“同步历史数据”按钮：可一键拉取 `BTC / ETH / SOL / BNB / DOGE` 在 `5m / 15m / 1H / 4H` 四个周期的全量历史缓存，并显示同步进度与完成摘要。
- 已把回测 `K` 线数量 `0` 正式定义为“全量历史”，并同步更新 `D:\qqokx\okx_quant\backtest.py`、`D:\qqokx\okx_quant\backtest_export.py`、`D:\qqokx\okx_quant\backtest_ui.py` 的校验、报告文案、对比详情与数据来源说明。
- 已补强 `D:\qqokx\okx_quant\okx_client.py`：`get_candles_history` 与 `get_candles_history_range` 现已支持 `limit=0` 全量取数，并把完整历史缓存写回 `D:\qqokx_data\cache\candle_cache\`，同时记录“全量历史 / 区间全量”统计信息。
- 已补充 `D:\qqokx\tests\test_backtest.py` 与 `D:\qqokx\tests\test_candle_cache.py`，覆盖全量回测、全量导出、全量缓存拉取与详情展示。
- 已完成本轮验证：`python -m py_compile okx_quant\backtest_ui.py okx_quant\backtest.py okx_quant\okx_client.py okx_quant\backtest_export.py tests\test_backtest.py tests\test_candle_cache.py`、`python -m unittest tests.test_backtest tests.test_candle_cache`（`85` 项测试通过）。
### v0.4.01 | 2026-04-22 | 启动追单窗口提示文案优化与版本线切换
- 已优化 `D:\qqokx\okx_quant\ui.py` 中“启动追单窗口(秒)”的提示文案：改为 `0=不追单；300=只追启动前5分钟内刚确认的信号`，让默认行为与常用配置示例更直观。
- 本次发版已将版本线切换到 `v0.4.01`，作为后续阶段版本起点。
- 已完成本轮验证：`python -m py_compile D:\qqokx\okx_quant\ui.py`、`python -m unittest tests.test_ui`（`30` 项测试通过）。
### v0.3.106 | 2026-04-22 | 启动追单窗口、波段接管与多轮继续监控
- 已为 `D:\qqokx\okx_quant\models.py` 增加启动追单窗口参数，并在 `D:\qqokx\okx_quant\engine.py` 的动态委托/动态信号链路里统一接入：启动时默认不追老信号，也支持在限定秒数内接管刚确认的当前波段。
- 已补强 `D:\qqokx\okx_quant\engine.py` 的交易所托管止盈止损模式：单轮持仓结束后继续监控下一次信号，不再在首次成交后直接结束；同时继续遵守每波开仓次数上限，避免同波段重复追单。
- 已同步更新 `D:\qqokx\okx_quant\ui.py`：新增启动追单窗口输入与摘要展示，快照恢复会保存该参数，运行状态也能识别“启动不追老信号 / 窗口过期 / 本轮持仓结束继续监控”等新日志。
- 已补充 `D:\qqokx\tests\test_strategy_engine.py` 与 `D:\qqokx\tests\test_ui.py`，覆盖启动追单窗口判定、单轮持仓结束后继续监控、以及运行状态映射。
- 已完成本轮验证：`python -m py_compile D:\qqokx\okx_quant\engine.py D:\qqokx\okx_quant\models.py D:\qqokx\okx_quant\ui.py D:\qqokx\tests\test_strategy_engine.py D:\qqokx\tests\test_ui.py`、`python -m unittest tests.test_strategy_engine tests.test_ui`（`67` 项测试通过）。
### v0.3.105 | 2026-04-22 | 实盘日志与 OKX 报错乱码清理
- 已清理 `D:\qqokx\okx_quant\engine.py` 中 `EMA5/EMA8` 实盘策略相关乱码文案：包括信号监控、运行模式、无信号提示、动态止损触发、准备下单、开仓成交、委托追踪等日志与提示，统一恢复为正常中文。
- 已清理 `D:\qqokx\okx_quant\okx_client.py` 中用户可见的乱码文案：包括 `算法委托`、`OKX 未返回盘口`、期权不支持“市价/限价附带止盈止损下单”的报错，以及 `OKX API 错误 code=...` 的兜底提示。
- 已同步更新 `D:\qqokx\tests\test_strategy_engine.py` 与 `D:\qqokx\tests\test_okx_client_orders.py`，修复测试中的乱码样例与断言，并补充 `get_order_book` 空返回报错文案、算法委托来源文案的覆盖。
- 已完成本轮验证：`python -m py_compile D:\qqokx\okx_quant\engine.py D:\qqokx\okx_quant\okx_client.py D:\qqokx\tests\test_strategy_engine.py D:\qqokx\tests\test_okx_client_orders.py`、`python -m unittest D:\qqokx\tests\test_strategy_engine.py D:\qqokx\tests\test_okx_client_orders.py`（`39` 项测试通过）。
### v0.3.104 | 2026-04-22 | 数据目录统一、增强策略保守恢复与打包说明同步
- 已新增 `D:\qqokx\okx_quant\app_paths.py`，统一数据根目录解析；默认把运行期数据放到代码目录同级 `qqokx_data/`，并支持 `python main.py --data-dir ...` 与环境变量 `QQOKX_DATA_DIR` 指定数据目录。
- 已收口 `D:\qqokx\main.py`、`D:\qqokx\okx_quant\persistence.py`、`D:\qqokx\okx_quant\log_utils.py` 的路径入口：配置、缓存、状态、日志、导出不再散落在代码目录，首次启动会兼容迁移旧版 `.okx_quant_*`、`logs/`、`reports/`。
- 已把 `D:\qqokx\okx_quant\deribit_volatility_ui.py` 与 `D:\qqokx\okx_quant\option_strategy_ui.py` 的 Deribit 缓存/导出路径切到统一数据目录，保证升级后可直接复用旧数据文件夹。
- 已补强 `D:\qqokx\okx_quant\enhanced_live_engine.py` 的运行快照：增强策略会持久化会话目录、`state.json`、`manifest.json`、事件流、活跃仓位、去重键、订单 ID 集，避免恢复后重复识别旧信号或旧成交。
- 已在 `D:\qqokx\okx_quant\ui.py` 接入增强策略“保守恢复”链路：应用关闭后会把会话标记为 `待恢复`，下次启动自动加载并尝试接管，同时新增 `恢复选中策略` 按钮支持手动恢复。
- 已为 `D:\qqokx\okx_quant\engine.py` 增加 `wait_stopped()`，用于关闭应用时等待线程更稳妥收口；普通策略当前仍不承诺自动恢复，`Smart Order` 继续沿用原有持久化恢复链路。
- 已同步更新 `D:\qqokx\scripts\build_server_package.py`、`D:\qqokx\README.md`、`D:\qqokx\软件开发指南.md`：明确共享数据目录、升级时保留 `qqokx_data/` 的推荐方式，以及增强策略恢复接管边界。
- 已完成本轮验证：`python -m compileall okx_quant tests`、`python -m py_compile main.py scripts\build_server_package.py okx_quant\app_paths.py`、`python -m unittest tests.test_enhanced_live_engine tests.test_strategy_engine tests.test_persistence tests.test_log_utils tests.test_ui tests.test_deribit_volatility_ui`（`105` 项测试通过）。
### v0.3.103 | 2026-04-21 | API环境同步、读超时容错与运行状态细化
- 已修正 `D:\qqokx\okx_quant\ui.py` 与 `D:\qqokx\okx_quant\persistence.py` 的多 API 配置切换链路：切换 `API` profile 时，会同步回填对应 `demo/live` 环境，不再出现 `API Key` 已切换但顶栏摘要和有效环境缓存仍沿用上一个配置的问题。
- 已把 `environment` 正式并入 `D:\qqokx\.okx_quant_credentials.json`；旧格式若只有 `environment_label` 也可兼容迁移读取。环境切换现在会进入自动保存队列，同时清空 `_positions_effective_environment`，避免账户持仓、账户信息、当前委托、历史委托继续沿用上一个账号的环境缓存。
- 已补强 `D:\qqokx\okx_quant\okx_client.py` 与 `D:\qqokx\okx_quant\engine.py` 的读请求容错：`The read operation timed out`、`handshake timed out` 等原生超时会先统一归一为可识别网络错误，再进入读取重试链路，减少瞬时读超时直接把实盘策略打停。
- 已为 `D:\qqokx\okx_quant\engine.py` 中 `OKX` 动态止损监控增加连续失败保护：单次或偶发读取失败时，仅记录“保留当前 OKX 止损并继续重试”；只有连续读取失败达到 `6` 次，才会停止并明确提示人工检查。
- 已把运行中策略状态细化为最近运行阶段展示：主界面会直接显示 `启动中`、`等待信号`、`开仓监控中`、`持仓监控中` 等状态，并在详情中附带 `最近运行状态 / 最近日志`，便于快速判断线程卡在什么阶段。
- 已统一把 `时间保本` 默认值收口为关闭，涉及 `D:\qqokx\okx_quant\models.py`、`D:\qqokx\okx_quant\ui.py`、`D:\qqokx\okx_quant\backtest_ui.py`，让主界面、回测界面与回测配置回填口径保持一致。
- 已完成本轮验证：`python -m compileall okx_quant tests`、`python -m unittest tests.test_persistence_credentials tests.test_strategy_engine tests.test_ui`（`69` 项测试通过）。

### v0.3.102 | 2026-04-21 | 动态挂单撮合修正、时间保本与停策略自动撤单
- 已修正 `EMA 动态委托` 回测撮合口径：下一根 `K` 线开盘若已优于挂单价，则按开盘立即成交；同步按 `Taker` 费率统计，并修正同根 `K` 线开仓后的路径价处理。涉及 `D:\qqokx\okx_quant\backtest.py`、`D:\qqokx\tests\test_backtest.py`。
- 已重建 `BTC / SOL 4H` 动态挂单新基准，旧口径作废；后续统一以修正后回测结果为准，并输出 `D:\qqokx\reports\backtest_exports\baseline_20260421_133436_ema_dynamic_fill_fix_btc_sol_4h.summary.md` 等新基准文件。
- 已完成 `SOL 4H / SL x1 / 动态止盈` 持仓 `K` 线根数统计与时间保本候选分析，生成 `D:\qqokx\reports\backtest_exports\analysis_sol_4h_short_sl1_dynamic_tp_holding_bars_20260421.json`、`D:\qqokx\reports\backtest_exports\analysis_sol_4h_short_sl1_dynamic_tp_time_stop_candidates_20260421.json`。
- 已为动态止盈策略新增 `时间保本` 规则：持仓满指定 `K` 线后，若价格已达到净保本区间，则把止损合并到开仓价 ± `2` 倍 `Taker` 手续费偏移，且止损只收紧、不回退。涉及 `D:\qqokx\okx_quant\models.py`、`D:\qqokx\okx_quant\backtest.py`、`D:\qqokx\okx_quant\engine.py`。
- 已同步把时间保本参数接入主界面与回测界面：新增 `启用时间保本`、`时间保本K线数`，并打通摘要、快照与审计透传。涉及 `D:\qqokx\okx_quant\ui.py`、`D:\qqokx\okx_quant\backtest_ui.py`、`D:\qqokx\okx_quant\backtest_audit.py`。
- 已增强 `D:\qqokx\okx_quant\ui.py` 的停策略链路：手动停止实盘策略时，先进入“停止中”，再检查并撤销该策略自己发出的未成交程序单；只有清理流程完成后，策略状态才真正落到“已停止”。
- 已补齐停止清理日志与人工接管提醒：会分别记录已提交撤单、撤单失败、残留未撤委托、检测到已成交委托、检测到仍有仓位、检测到保护委托，以及最终停止结论；若未完全收口，会明确提示人工检查 `当前委托 / 账户持仓 / OKX 托管止损`。
- 已补充并通过本轮验证：`python -m compileall okx_quant tests`、`python -m unittest tests.test_backtest tests.test_strategy_engine tests.test_ui`（`132` 项测试通过）。

### v0.3.101 | 2026-04-18 | 刷新失败摘要化、旧缓存保护与状态徽标
- 已将 `D:/qqokx/okx_quant/ui.py` 中持仓、账户信息、当前委托、历史委托的刷新异常统一接入摘要化处理：像 OKX `HTTP 502 / Cloudflare` 这类整页 HTML 错页不再原样灌进日志，而是收口为 `HTTP 502 | OKX源站异常 | RayID=...` 这类可读摘要。
- 已为持仓、账户信息、当前委托、历史委托增加刷新健康度跟踪：记录上次成功时间、连续失败次数、最近错误摘要；连续失败达到 `3` 次且仍在沿用旧缓存时，会明确标记“数据可能已过期”。
- 已把“继续显示上一份缓存”从持仓扩展到账户信息 / 当前委托 / 历史委托，并在摘要中补充连续失败次数、上次成功时间与缓存年龄，方便快速判断是不是交易所临时抖动还是已经进入过期态。
- 已为依赖实时缓存的关键动作增加保护：当 `当前委托` 已进入过期态时，会拦截单笔撤单和批量撤单；当 `持仓` 已进入过期态时，会拦截启动持仓保护。拦截时会自动再发起一次刷新，避免基于旧缓存直接执行危险操作。
- 已为主界面持仓、持仓大窗持仓、账户信息、当前委托、历史委托补充可视化刷新状态徽标：`未读`（灰）/ `正常`（绿）/ `告警 xN`（黄）/ `过期 xN`（红），让远程/VPN 抖动场景下一眼就能判断当前数据是否可靠。
- 已补充并通过本轮验证：`python -m compileall okx_quant tests`、`python -m unittest discover -s tests -p test_ui.py`（16 项测试通过），覆盖 Cloudflare 502 摘要化、刷新健康度状态流转与状态徽标文案。

### v0.3.100 | 2026-04-18 | 现货增强三十六计主界面接入、持仓保护降噪与标准R倍数修正
- 已将 `现货增强三十六计` 作为 live-only 策略接入主界面启动链路，并将回测窗口候选策略与实盘候选策略分离；选中该策略时打开回测会自动回退到可回测策略，避免启动配置错位。
- 已新增 `D:/qqokx/okx_quant/enhanced_live_engine.py`：支持现货信号标的与永续交易标的自动换算、信号源 K 线加载、人工池优先减仓与会话产物落盘，为主界面直接启动“现货增强三十六计”提供执行引擎。
- 已在 `D:/qqokx/okx_quant/ui.py` 与 `D:/qqokx/okx_quant/strategy_catalog.py` 补齐“现货增强三十六计”参数校验与默认值：`固定数量` 作为单槽数量，`每波最多开仓次数` 作为每个方向最大槽位数，并收口为“交易并下单”模式。
- 已为 `D:/qqokx/okx_quant/position_protection.py` 增加监控心跳节流：连续“监控中”状态默认每 `30s` 才写一次日志，但状态消息仍实时刷新；若刚经历网络异常等非监控状态，恢复监控后会立即补打一条心跳，减少持仓保护日志刷屏。
- 已修复回测标准 `R` 倍数口径：`D:/qqokx/okx_quant/backtest.py` 新增 `_position_initial_risk_value`，未平仓展示、人工池持仓和已平仓交易统一按开仓价到初始止损的原始风险计算，不再因止损上移而放大 `R` 值。
- 已补充 `D:/qqokx/tests/test_backtest.py`、`D:/qqokx/tests/test_enhanced_live_engine.py` 与 `D:/qqokx/tests/test_position_protection.py` 回归覆盖，并完成本轮验证：`python -m unittest tests.test_backtest tests.test_enhanced_live_engine tests.test_position_protection`（106 项测试通过）与 `python -m py_compile okx_quant\\backtest.py okx_quant\\strategy_catalog.py okx_quant\\ui.py okx_quant\\enhanced_live_engine.py okx_quant\\position_protection.py tests\\test_backtest.py tests\\test_enhanced_live_engine.py tests\\test_position_protection.py`。

### v0.3.99 | 2026-04-17 | 历史策略溯源、独立日志与清理能力
- 已新增历史策略持久化文件 `D:/qqokx/.okx_quant_strategy_history.json`：策略启动、手动停止、线程自然结束、程序关闭与异常退出恢复都会同步落盘，记录会话号、参数快照、状态、结束原因与独立日志路径，方便追溯。
- 已新增“历史策略”窗口查看链路：支持历史列表、详情查看、打开日志、复制日志路径，便于直接定位某次实盘会话的执行记录。
- 已为每次策略会话补齐独立日志文件，统一写入 `D:/qqokx/logs/strategy_sessions/`；总日志与会话独立日志会同时保留，历史记录中会同步保存 `log_file_path`。
- 已在“运行中策略”区域新增 `清空已停止` 按钮，并补充二次确认、清空后自动选中剩余会话逻辑；该操作只清主界面列表，不删历史策略记录和独立日志。
- 已在“历史策略”窗口新增 `删除选中` 与 `清空历史`；默认只删除历史记录，不删除独立日志；仍对应运行中或停止中会话的记录会自动保留，避免被后台线程再次写回。
- 已完成本轮验证：`python -m py_compile D:/qqokx/okx_quant/ui.py D:/qqokx/okx_quant/persistence.py D:/qqokx/okx_quant/log_utils.py D:/qqokx/tests/test_persistence.py D:/qqokx/tests/test_log_utils.py D:/qqokx/tests/test_ui.py`、`python -m unittest D:/qqokx/tests/test_persistence.py D:/qqokx/tests/test_log_utils.py D:/qqokx/tests/test_ui.py`（30 项测试通过），以及历史策略窗口/主窗口 UI smoke 验证。

### v0.3.98 | 2026-04-17 | 1H 无信号阶段降噪与空窗降频
- 已对 `EMA 动态委托` 实盘循环补充“同一根已确认 K 线仅记录一次无信号日志”控制，避免 `1H` 策略在同一根 K 线下按轮询秒数重复输出“当前无法生成挂单 / 当前无法生成动态开仓价”。
- 已为无活动挂单、无本地触发条件的空窗阶段增加保守降频：等待时长会结合 `K` 线周期、下一根收线前剩余时间与 `60s` 上限自动计算，减少无意义的高频重算，同时不影响临近新收线时的重新评估。
- 已保留执行态高频链路：存在活动挂单、本地触发条件或动态止损上移监控时，仍继续按原 `poll_seconds` 频率执行，不降低成交确认、撤单同步与 OKX 止损上移响应。
- 已补充并通过本轮回归：`python -m py_compile D:\qqokx\okx_quant\engine.py D:\qqokx\tests\test_strategy_engine.py` 与 `python -m unittest D:\qqokx\tests\test_strategy_engine.py`，新增覆盖空窗降频与“同 K 线只记一次日志”的测试场景。

### v0.3.97 | 2026-04-17 | 增强回测实验室、委托视图与 OKX 容错加固
- 已统一补强实盘策略的 OKX 容错：标的信息、K 线、订单状态、挂单列表、持仓列表、触发价、ATR 快照与成交价估算统一接入受控重试；瞬时网络异常会自动重试，业务拒绝与参数错误仍直接抛出。
- 已补齐策略引擎、智能下单与持仓保护的写入恢复链路：下单、撤单、平仓与算法止损改单在响应丢失时会先按 `clOrdId / algoClOrdId` 回查，必要时使用同一 `clOrdId` 补发一次，避免任务被瞬时网络抖动直接打断。
- 已继续加固 `动态止盈 + OKX 托管`：补齐 ASCII 安全 `clOrdId`、候选止损有效性判断、按 `algoClOrdId` 定位算法委托与动态上移止损恢复，`ETH-USDT-SWAP` 模拟盘整链路已完成复核且无残留挂单、无残留持仓。
- 已将 `账户信息` 窗口扩展为 `资产明细 / 当前委托 / 历史委托` 标签页结构，并新增 `刷新全部` 入口；用户不必再切换到持仓大窗也能查看、刷新和撤销账户委托。
- 已将当前委托/历史委托表格、筛选、详情与撤单链路抽为可复用组件，`账户信息` 与 `持仓大窗` 共用同一套数据源和列定义；多窗口并行显示时，刷新后会同步更新两处视图且不会继续引用已关闭窗口的失效控件。
- 已补齐智能下单在 `long_short` 模式下的 `posSide` 处理：网格单、条件触发单、止盈止损平仓和激进成交路径都会按多空方向带出正确仓位侧，并补充掉单回查与补发测试。
- 已落地现货增强三十六计回测实验室底座：新增子策略注册、信号聚合、门控规则、额度引擎、运行时配置热切换、样例策略包、分段历史加载与增强回测执行链路，便于持续迭代现货增强策略。
- 已补齐持仓与证据审计导出：新增 `position_ledger.csv`、`position_ledger_summary.csv`、`manual_review_manifest.csv`、`evidence_index.csv`、`evidences.json`、证据 K 线小图、人工接管画廊、`position_management.html`、`total_position_management.html` 与 `total_position_summary.csv`，并支持多种总仓合并口径与价格域隔离。
- 已新增增强实验室报告脚本 `scripts/generate_enhanced_seed_lab_smoke_report.py` 与 `scripts/generate_enhanced_seed_lab_realdata_report.py`，便于生成冒烟报告与真实样例审计材料。
- 已补充并通过本轮核心回归：`python -m unittest tests.test_strategy_engine tests.test_smart_order tests.test_okx_client_orders tests.test_enhanced_strategy_foundation tests.test_enhanced_strategy_lab`，并完成 `python -m py_compile` 语法校验。

### v0.3.96 | 2026-04-17 | 实盘标的收口与 OKX 动态止损联动
- 实盘启动区已统一为单一`交易标的`心智：移除单独的`下单标的`选择，运行中列表与启动确认文案同步收口；内部仍保留`inst_id / trade_inst_id`兼容位，便于后续扩展到期权或跨标的场景。
- 实盘标的输入已全部收紧为 OKX 正式永续格式与下拉选择：默认值改为`ETH-USDT-SWAP`，`信号/下单/自定义触发标的`均改为只读列表；`加载 OKX SWAP`后会同步刷新候选项。
- 实盘默认参数已调整为更保守的起始值：`风险金=10U`、`止损 ATR=2`，并同步修正 EMA5/8 分支的界面默认值与启动时内部兜底值，避免界面与真实执行不一致。
- 已打通`动态止盈 + OKX 托管`执行链路：动态止盈模式下开仓时仅向 OKX 附加初始止损，不再预挂固定止盈；成交后策略持续监控价格，并按现有`2R保本 / 手续费偏移 / nR 锁定 (n-1)R`规则动态调用 OKX 算法单改单接口上移止损。
- 已补充 OKX 算法止损改单能力与日志链路：为挂单附加独立`algoClOrdId`，新增`amend-algos`封装、持仓存活检测和动态止损上移日志；同时移除 UI 中“动态止盈不能选 OKX 托管”的限制。
- 已修复策略引擎`clOrdId`生成问题：原先会从中文策略名中带入非 ASCII 字符，导致 OKX 拒单并报“操作全部失败”；现已收紧为仅生成 ASCII 字母数字订单号，避免 UI 启动策略时出现隐性拒单。
- 已修复 OKX 动态止损上移竞态：成交后先按`algoClOrdId`确认算法止损委托已进入挂单列表，再携带`algoId + algoClOrdId`执行`amend-algos`；若交易所侧委托尚未就绪，会自动重试而不是直接停掉策略。
- 已完成`ETH-USDT-SWAP`模拟盘 UI 级整链路验证：从主界面`启动`按钮创建策略会话，到自动提交挂单、成交、附加 OKX 止损、动态上移止损，整条链路已实测跑通；验证结束后已确认无残留持仓、无残留挂单。
- 已补充并通过本轮回归：`python -m unittest D:\qqokx\tests\test_strategy_engine.py D:\qqokx\tests\test_okx_client_orders.py`，覆盖`clOrdId` ASCII 限制、按`algoClOrdId`定位算法委托与 OKX 止损改单请求格式。

### v0.3.100 | 2026-04-21 | 时间保本专项回测、默认值收口与回测窗口优化
- 已清理历史失准回测产物：按要求删除 `D:/qqokx/reports/analysis` 与 `D:/qqokx/reports/backtest_exports` 下旧版回测报告/导出，避免旧口径结果继续混用。
- 已新增专项脚本 `D:/qqokx/scripts/generate_dynamic_time_break_even_report.py`，并按新版双滑点口径完成 `BTC/ETH/BNB/SOL/DOGE`、`5m/15m/1H/4H`、多空、`EMA21/55`、`SL 1/1.5/2 ATR`、`每波开仓 0/1/2/3`、`时间保本 0/5/10/15/20` 的 `4800` 组动态止盈全面回测。
- 已生成新版中文纯文本/CSV 报告：包括 `新版全面回测总报告_动态止盈_时间保本_20260421_162724.txt`、全量结果、最终最佳、按开仓次数最佳、按时间保本最佳、时间保本按周期/币种汇总、一页版领导摘要与首批实盘建议/5000U UI 逐项填写表。
- 已根据最新回测结果收口默认值：`时间保本` 默认改为关闭，涉及 `D:/qqokx/okx_quant/models.py`、`D:/qqokx/okx_quant/ui.py`、`D:/qqokx/okx_quant/backtest_ui.py`；当前建议全局默认 `时间保本=0`、`每波开仓次数=1`。
- 已优化回测窗口首屏布局：`D:/qqokx/okx_quant/backtest_ui.py` 提高默认打开高度与最小高度，并增大底部图表区权重，回测窗口首次打开时最下方图表区域会露出更多内容。
- 已修复 `D:/qqokx/okx_quant/okx_client.py` 中一组损坏字符串/`f-string` 导致的启动级语法错误，恢复主程序可启动状态，并完成 `python -m compileall okx_quant tests` 校验通过。

### v0.3.104 | 2026-04-22 | 动态委托连续运行、启动不追老信号与追单窗口
- 已将 `D:/qqokx/okx_quant/engine.py` 的 `EMA 动态委托` OKX 托管实盘链路改为连续运行模式：本轮挂单成交、止损/止盈结束后，不再整条线程直接停止，而是记录“本轮持仓已结束，继续监控下一次信号”，再回到外层继续盯下一次机会。
- 已把 `每波最多开仓次数` 正式补齐到 OKX 托管链路，并与本地触发/只监控信号链路统一：同一波趋势内会累计开仓次数；当额度耗尽时，只等待当前波失效或方向翻转，不会在同一波内反复重开。
- 已新增“启动默认不追老信号”规则：动态策略启动时，若当前波段的确认时点早于线程启动时刻，则默认跳过这波；只有等当前波失效，或后续形成新波，才重新恢复开仓/发信号资格。
- 已新增可选参数 `启动追单窗口(秒)`，并接入 `D:/qqokx/okx_quant/models.py`、`D:/qqokx/okx_quant/ui.py`、`D:/qqokx/okx_quant/engine.py` 的收集、确认、快照与执行链路：默认值为 `0`（即启动不追老信号）；若设置正数，且启动前刚确认的波段仍落在窗口内，则允许接管当前波段。
- 已把上述启动门控同时应用到 `OKX 托管`、`本地触发下单`、`只监控信号` 三条动态策略链路，确保实盘与信号监控口径一致。
- 已同步补强运行状态识别：`D:/qqokx/okx_quant/ui.py` 现在会把“启动默认不追老信号 / 启动追单窗口已过期 / 本轮持仓已结束 / 开仓次数已达上限”等日志统一识别为 `等待信号`，避免运行中策略状态停留在旧阶段。
- 已补充并通过本轮验证：`python -m compileall D:/qqokx/okx_quant D:/qqokx/tests`、`python -m unittest D:/qqokx/tests/test_strategy_engine.py D:/qqokx/tests/test_ui.py`（`67` 项测试通过）。

### v0.4.02 | 2026-04-22 | 旧诊断文件与临时脚本清理
- 已删除 `D:/qqokx/_mojibake_report.json`；该文件为一次性乱码排查报告，不属于程序运行数据，也没有被代码引用。
- 已盘点 `D:/qqokx/scripts/` 并清理明确的临时查询脚本：`tmp_deep_backtest_summary.py`、`tmp_query_15m.py`、`tmp_query_5m.py`、`tmp_query_long_short_best.py`、`tmp_query_summary.py`、`tmp_query_summary2.py`、`tmp_query_summary3.py`、`tmp_query_summary4.py`。
- 已同时清理两份依赖旧目录结构的专项分析脚本：`D:/qqokx/scripts/generate_btc_regime_report.py`、`D:/qqokx/scripts/generate_ema2155_three_cycle_reports.py`；两者仍写死旧 `.okx_quant_candle_cache` 与旧 `reports/analysis` 路径，且未被当前代码或文档流程引用。
- 已保留 `D:/qqokx/scripts/build_server_package.py` 及现有动态止盈/增强实验室/槽位接管等正式报告脚本，不影响当前打包、回测分析与实验报告链路。


### v0.4.02 | 2026-04-22 | 补充清理：Codex 本地目录与过期锁 1 报告脚本
- 已删除 `D:/qqokx/.codex/`；该目录仅包含 Codex 自动生成的本地环境配置 `environments/environment.toml` 等，不属于项目运行或发布所需文件。
- 已删除 `D:/qqokx/scripts/generate_dynamic_lock1_backtest_report.py`；该脚本依赖旧 `reports/analysis/comprehensive_backtest_all_runs_20260415_112346.csv` 固定基线文件，属于旧版单次研究脚本，且未被当前代码、README 或发布流程引用。
- 已保留其余仍可能复用的正式/半正式报告脚本，包括 `generate_dynamic_time_break_even_report.py`、`generate_comprehensive_backtest_report.py`、`generate_dynamic_fee_offset_report.py`、`generate_dynamic_two_r_break_even_report.py`、`generate_ema_dynamic_reports.py`、增强实验室报告脚本与槽位接管报告脚本；后续若要继续瘦身，建议再按“是否迁移到新数据目录”单独筛一轮。


### v0.4.02 | 2026-04-22 | 保留脚本迁移到新数据目录
- 已将剩余保留的回测/分析脚本统一迁移到共享数据目录 `D:/qqokx_data`：`generate_comprehensive_backtest_report.py`、`generate_dynamic_fee_offset_report.py`、`generate_dynamic_time_break_even_report.py`、`generate_dynamic_two_r_break_even_report.py`、`generate_ema_dynamic_reports.py`、`generate_slot_handoff_strategy_pool_report.py`、`generate_enhanced_seed_lab_realdata_report.py`、`generate_enhanced_seed_lab_smoke_report.py`。
- 已把上述脚本的输出目录从旧 `D:/qqokx/reports` / `reports/analysis` / `reports/backtest_exports` 迁到新的 `analysis_report_dir_path()`、`reports_dir_path()`、`backtest_report_export_dir_path()` 等统一路径入口，避免脚本继续写回旧目录。
- 已将 `generate_enhanced_seed_lab_realdata_report.py` 的默认运行时配置文件从旧根目录 `.okx_quant_enhanced_strategy_runtime.json` 切换为 `D:/qqokx_data/config/enhanced_strategy_runtime.json`，与当前程序主体保持一致。
- 已补充清理 `D:/qqokx/scripts/__pycache__/` 本地缓存目录；经本轮复核，当前保留脚本里已不再存在旧 `reports` 或旧 `.okx_quant_*` 数据路径写入点（打包说明文本中的兼容迁移文案除外）。


### v0.4.02 | 2026-04-22 | 补充清理：tests_artifacts
- 已删除 `D:/qqokx/tests_artifacts/`；该目录内仅包含测试预览图、合成图与 `.ps` 调试产物，不属于项目运行或发布所需文件。
- `D:/qqokx/tests/test_persistence.py` 仅把 `tests_artifacts/` 作为测试临时工作区根目录；后续如再次运行相关测试，会按需自动重建随机子目录，不影响程序主体。

### v0.4.04 | 2026-04-23 | 运行中策略参数导入导出
- 已为 `D:/qqokx/okx_quant/ui.py` 新增策略参数模板导入导出能力：运行中策略列表区域新增 `导出选中参数`、`导入策略参数` 按钮，工具菜单也同步新增 `导出选中策略参数` 与 `导入策略参数并启动` 入口。
- 已新增策略模板结构与序列化/反序列化链路：通过 `StrategyTemplateRecord`、`_build_strategy_template_payload(...)`、`_strategy_template_record_from_payload(...)` 导出运行中策略的 `config_snapshot`，可在另一台机器或切换不同 API 后重新导入并复用同一套启动参数。
- 导入时已处理 API 差异：模板文件不包含 API 密钥；若本机存在同名 API 配置则自动切换，否则保留当前已选 API，并在导入提示中明确说明，避免误以为跨机器会带出凭证。
- 导入后会自动回填启动区关键参数：包括策略、标的、K线周期、方向、EMA/ATR、风险金、固定数量、止盈止损模式、动态止盈相关参数、启动追单窗口、环境等，并支持导入后直接确认启动。
- 已补充并通过本轮验证：`python -m py_compile D:/qqokx/okx_quant/ui.py D:/qqokx/tests/test_ui.py` 与 `python -m unittest D:/qqokx/tests/test_ui.py`（39 项测试通过），覆盖模板 round-trip、API 选择回退与导入参数回填逻辑。

### v0.4.05 | 2026-04-24 | 重复策略启动拦截与风险高亮
- 已为 `D:/qqokx/okx_quant/ui.py` 增加重复启动硬拦截：当同一 `API` 下已经存在“同参数、同标的”的策略会话，且状态属于 `运行中 / 停止中 / 待恢复 / 恢复中` 时，再次启动会直接阻止，避免同一套实盘参数被重复运行而造成重复下单事故。
- 已把重复判定同步接入参数导入链路：导入策略参数后，若与当前运行中的某条策略完全重复，会自动定位到冲突会话并弹出提示，不再继续给出直接启动的误导流程。
- 已为运行中策略列表增加重复风险可视提示：冲突会话会以 `duplicate_conflict` 样式高亮显示；顶部“多策略合计”摘要也会追加 `重复风险 x组/x条` 统计，方便快速发现危险会话组合。
- 已在选中策略详情中补充重复风险说明：会明确显示与哪些会话参数完全相同，并提示“如需复制参数开新策略，请先修改标的或切换 API 后再启动”，减少误操作。
- 已补充并通过本轮验证：`python -m py_compile D:/qqokx/okx_quant/ui.py D:/qqokx/tests/test_ui.py` 与 `python -m unittest D:/qqokx/tests/test_ui.py`（46 项测试通过），覆盖重复会话识别、列表高亮、导入后冲突提示与启动前引导文案。

### v0.4.06 | 2026-04-24 | 交易员管理台、运行控制与价格开关修正
- 已新增交易员管理台运行底座：补充 `D:/qqokx/okx_quant/trader_desk.py` 与 `D:/qqokx/okx_quant/trader_desk_ui.py`，支持交易员草稿、运行态、额度格槽位、事件日志、亏损记录、均价汇总、价格开关与快照持久化；主界面 `D:/qqokx/okx_quant/ui.py` 已接入交易员启动、暂停、恢复、手动平仓与 watcher/槽位联动。
- 已按“固定数量补位”收口交易员策略执行口径：交易员下单不再依赖策略内部风险金动态换算，而是以固定数量逐格开仓；盈利平仓后释放额度格并继续补位，止损单会保留成本、开仓时间、净盈亏、结束原因等记录，便于在交易员台内追踪均价和后续人工处理。
- 已补齐交易员台 UI：支持加入当前参数、保存草稿、复制到目标交易标的/触发标的、期权合约目标复制、价格开关启用与条件设置、槽位记录窗口、事件日志窗口、累计净盈亏与当前均价展示；价格开关条件已改为中文加符号显示：`始终开启`、`高于 >=`、`低于 <=`、`区间内 [下限, 上限]`。
- 已修复交易员台若无已平仓记录时的累计净盈亏崩溃：`D:/qqokx/okx_quant/trader_desk.py` 的空统计结果现在固定返回 `Decimal("0")`，`D:/qqokx/okx_quant/trader_desk_ui.py` 的盈亏格式化也补了非 `Decimal` 兜底，避免出现 `AttributeError: 'int' object has no attribute 'quantize'`。
- 已修复 watcher 异常停止后反复自启的问题：`D:/qqokx/okx_quant/ui.py` 现在会识别真实停止原因；若 watcher 或活动额度格意外结束，会暂停对应交易员并记录明确事件，不再出现 `S02 -> S04 -> S06` 这类循环重启刷屏。
- 已修复交易员台右侧表单被定时刷新覆盖的问题：`D:/qqokx/okx_quant/trader_desk_ui.py` 现已加入表单脏状态保护；同一条交易员草稿在用户编辑时，自动刷新只更新列表/日志，不会再把“价格开关”等未保存选择强制刷回 `始终开启`。
- 已补充并通过本轮验证：`python -m unittest tests.test_trader_desk tests.test_trader_desk_ui tests.test_ui`、`python -m unittest tests.test_ui tests.test_trader_desk_ui`、`python -m py_compile D:/qqokx/okx_quant/trader_desk_ui.py D:/qqokx/okx_quant/ui.py`。


### v0.4.07 | 2026-04-24 | 主界面运行中策略筛选与入口去重
- 已移除 `D:/qqokx/okx_quant/ui.py` 工具菜单中的重复入口：`打开策略历史`、`导出选中策略参数`、`导入策略参数并启动`；主界面按钮行 `历史策略 / 导出选中参数 / 导入策略参数` 保留，避免功能失去可用入口。
- 已为主界面“运行中策略”区域新增来源筛选下拉：`全部 / 普通量化 / 交易员策略 / 信号观察台`，可按会话来源快速过滤列表；摘要区会追加当前筛选显示条数。
- 已为运行中策略列表新增 `来源类型`、`周期` 两列；来源类型按会话 `trader_id` 与 `run_mode` 自动判定，周期显示当前会话的 `config.bar`。
- 已补充并通过本轮验证：`python -m py_compile D:/qqokx/okx_quant/ui.py D:/qqokx/tests/test_ui.py`、`python -m unittest tests.test_ui`（`52` 项测试通过）。

### v0.4.08 | 2026-04-24 | 交易员日志收口与会话编号显化
- 已将 `D:/qqokx/okx_quant/engine.py` 中非 `1H` 周期策略的 `1H参考调试` 启动日志彻底关闭；现在只有真实 `1H` 周期才会输出 `1H调试`，避免 `1m/5m` 交易员被跨周期参考日志误导。
- 已为主界面 `D:/qqokx/okx_quant/ui.py` 的“运行中策略”列表新增 `会话` 列，直接显示 `S01/S02...`；历史策略窗口也同步新增 `会话` 列，历史记录里的 `session_id` 不再只藏在详情文本里。
- 已为 `D:/qqokx/okx_quant/trader_desk_ui.py` 的交易员管理台主列表新增 `当前会话` 列；若同一交易员当前存在多路活动会话，会按 `S03 +2` 这类格式展示，方便快速看出当前 watcher/活动额度格数量。
- 已补充并通过本轮验证：`python -m unittest tests.test_strategy_engine tests.test_debug`、`python -m unittest tests.test_ui tests.test_trader_desk_ui`、`python -m py_compile D:/qqokx/okx_quant/engine.py D:/qqokx/okx_quant/ui.py D:/qqokx/okx_quant/trader_desk_ui.py D:/qqokx/tests/test_strategy_engine.py D:/qqokx/tests/test_debug.py D:/qqokx/tests/test_ui.py D:/qqokx/tests/test_trader_desk_ui.py`。

### v0.4.09 | 2026-04-24 | 信号观察台删除入口与文案收口
- 已为 `D:/qqokx/okx_quant/signal_monitor_ui.py` 的“运行中的 signal_only 会话”区域新增 `删除选中会话` 按钮，并补充确认弹窗；删除只作用于已停止的 `signal_only` 会话记录，运行中或停止中的会话会保留，避免误删监控线程。
- 已在 `D:/qqokx/okx_quant/ui.py` 补齐信号观察台专用删除回调：删除后会同步从主界面运行中策略列表移除对应已停止会话，并保持剩余会话的选中状态衔接。
- 已将 `D:/qqokx/okx_quant/signal_monitor_ui.py` 窗口内面向用户的“草稿”文案统一收口为“信号”，包括左侧分组标题、按钮、列表列名、状态栏、提示文案与日志提示，界面心智与线程命名保持一致。
- 已新增 `D:/qqokx/tests/test_signal_monitor_ui.py`，并完成本轮验证：`python -m unittest tests.test_signal_monitor_ui -v`、`python -m unittest tests.test_ui.UiHelpersTest -v`。

### v0.4.10 | 2026-04-24 | 交易员价格开关输入引导优化
- 已优化 `D:/qqokx/okx_quant/trader_desk_ui.py` 的价格开关表单交互：`高于 >=` 时仅启用 `触发价 >=` 输入框，`低于 <=` 时仅启用 `触发价 <=` 输入框，`区间内 [下限, 上限]` 时启用双阈值，`始终开启` 时两格统一置灰并显示“无需填写”提示。
- 已在价格开关切换时同步联动字段标签与输入状态，减少“高于/低于该填下限还是上限”的理解成本；新建草稿、切换草稿、自动刷新后都会维持正确的控件启用状态。
- 已补充保存时的阈值清洗逻辑：无关条件下的残留阈值会自动忽略，不会出现输入框已禁用但旧值仍被带入保存的隐形配置问题。
- 已补充并通过本轮验证：`python -m py_compile D:/qqokx/okx_quant/trader_desk_ui.py D:/qqokx/tests/test_trader_desk_ui.py`、`python -m unittest tests.test_trader_desk_ui`（16 项测试通过）。

### v0.4.11 | 2026-04-24 | 交易员槽位统计与平仓结果同步修正
- 已修复 `D:/qqokx/okx_quant/ui.py` 中交易员槽位平仓写回不一致的问题：当归因结果缺少 `net_pnl` 时，槽位状态此前会被标成 `closed_loss`，但 `slot.net_pnl` 仍被写成 `None`，导致槽位表、平仓统计和累计净盈亏出现“状态已结束但摘要未记账”的不同步现象；现在统一改为写回归一化后的净盈亏值，至少会落成 `0`。
- 已修复 `D:/qqokx/okx_quant/trader_desk.py` 的历史统计口径：对旧快照里已经落成 `closed_loss / closed_profit / closed_manual` 但 `net_pnl` 为空的槽位，统计层现在会按已平仓记录处理，并以 `0` 参与净盈亏与胜负计数，避免历史数据在 UI 摘要中被直接跳过。
- 已同步收口交易员事件日志中的盈亏显示：新产生的亏损单/盈利单事件不再出现 `净盈亏=-` 这类与槽位状态不一致的记录。
- 已补充并通过本轮验证：`python -m py_compile D:/qqokx/okx_quant/trader_desk.py D:/qqokx/okx_quant/ui.py D:/qqokx/tests/test_trader_desk.py D:/qqokx/tests/test_ui.py`、`python -m unittest tests.test_trader_desk tests.test_ui`（71 项测试通过）。

### v0.4.11 | 2026-04-24 | 信号观察台收口、旧增强体系下线与回测扩展统计按需显示
- 已将旧 `人工池 / 槽位接管 / 现货增强三十六计` 体系从主代码与主入口中下线：删除 `D:/qqokx/okx_quant/enhanced_*.py`、`D:/qqokx/okx_quant/strategies/ema_slot_handoff.py`、`D:/qqokx/okx_quant/signal_monitor.py` 及相关旧测试/脚本，并清理 `D:/qqokx_data/state/`、`D:/qqokx_data/reports/` 中对应的旧记录与产物。
- 已重写 `D:/qqokx/okx_quant/strategy_catalog.py`，主界面策略列表现仅保留正常策略入口：`EMA 动态委托做多`、`EMA 动态委托做空`、`EMA 穿越策略`、`EMA5/8 穿越止损`；原 `EMA 槽位接管做多/做空` 与 `现货增强三十六计` 已移除。
- 已将原“信号监控页”收口为 `D:/qqokx/okx_quant/signal_monitor_ui.py` 的 `信号观察台`：支持从主界面当前参数保存 `signal_only` 草稿、批量复制到多币种、批量启动/停止 `signal_only` 会话，并将草稿持久化到 `D:/qqokx_data/state/signal_observer_templates.json`。
- 已新增 `D:/qqokx/okx_quant/trader_desk_ui.py` 作为独立 `交易员管理台` 雏形：支持交易员草稿、`总额度 / 单次额度 / 额度次数 / 盈利单自动平仓 / 状态 / 备注` 的配置与持久化，数据文件为 `D:/qqokx_data/state/trader_desk.json`；该页面当前仅提供额度草稿层，不改变普通量化交易主流程。
- 已在 `D:/qqokx/okx_quant/ui.py` 接入两个新入口：`打开信号观察台`、`打开交易员管理台`；普通策略启动链路仍沿用现有正常量化交易逻辑，`signal_only` 会话可由观察台统一管理。
- 已继续收口 `D:/qqokx/okx_quant/backtest.py`、`D:/qqokx/okx_quant/backtest_export.py`、`D:/qqokx/okx_quant/backtest_ui.py`：移除旧“槽位接管”分支与残留文案；回测窗口中的 `扩展统计` 页签现改为按数据动态显示，普通策略默认不再出现空白页签，只有存在额外扩展数据时才显示。
- 已补充并通过本轮验证：`python -m py_compile D:/qqokx/okx_quant/ui.py D:/qqokx/okx_quant/signal_monitor_ui.py D:/qqokx/okx_quant/trader_desk_ui.py D:/qqokx/okx_quant/backtest.py D:/qqokx/okx_quant/backtest_ui.py D:/qqokx/okx_quant/backtest_export.py D:/qqokx/okx_quant/backtest_strategy_pool.py D:/qqokx/okx_quant/strategy_catalog.py D:/qqokx/okx_quant/persistence.py D:/qqokx/okx_quant/app_paths.py D:/qqokx/okx_quant/strategies/__init__.py D:/qqokx/tests/test_notifications.py D:/qqokx/tests/test_backtest.py D:/qqokx/tests/test_ui.py`；`python -m unittest tests.test_notifications tests.test_backtest tests.test_persistence tests.test_ui`（140 项通过，2 项因本地缺失可选 K 线缓存跳过）。

### v0.4.12 | 2026-04-25 | 交易员虚拟止损、邮件上下文补全与强制清格
- 已将交易员模式改为 `虚拟止损` 执行口径：`D:/qqokx/okx_quant/models.py` 新增 `trader_virtual_stop_loss` 配置位，`D:/qqokx/okx_quant/ui.py` 在交易员启动 watcher 时强制写入该标记；`D:/qqokx/okx_quant/okx_client.py` 的 `place_market_order(...)` / `place_limit_order(...)` 新增 `include_attached_protection` 参数，交易员下单会跳过 `attachAlgoOrds`，不再向 OKX 挂真实止损单。
- 已在 `D:/qqokx/okx_quant/engine.py` 为动态委托成交后新增交易员专用持仓监控分支：初始策略止损价只记录 `交易员虚拟止损已触发（不平仓）`，不会直接平仓释放额度；只有固定止盈、动态止盈保护价进入保本/盈利区后触发，或人工平仓时，才会真实执行平仓并释放额度格。启动日志与策略说明也已同步改成“交易员虚拟止损，不向 OKX 托管真实止损”的文案，避免 UI/日志与真实行为不一致。
- 已补充邮件通知上下文：`D:/qqokx/okx_quant/notifications.py`、`D:/qqokx/okx_quant/engine.py`、`D:/qqokx/okx_quant/ui.py` 现会在信号、成交、异常邮件里带上 `会话号 / 交易员编号 / API / 策略名 / 策略ID / 运行模式 / 规则方向 / 信号标的 / 下单标的 / K线周期` 等字段，并将 `long/short`、`buy/sell` 转成中文展示，便于直接从邮件判断是哪条交易员/策略触发。
- 已为 `D:/qqokx/okx_quant/trader_desk_ui.py` 新增 `强制清格` 按钮，并在 `D:/qqokx/okx_quant/ui.py` 增加 `force_clear_trader_draft(...)`：可在交易员台内一键清理残留 `watching/open/stopped/failed` 本地额度格、释放本地占用并尝试停止关联会话，同时明确记录“这不代表交易所真实仓位已平”。适用于交易员线程已停、但本地槽位状态卡死导致无法继续操作的场景。
- 已收口交易员删除流程：当 `辞退交易员` 发现界面上已无 `open/watching` 槽位、但仍被隐藏的本地残留状态拦住时，系统会自动先执行一次本地强制清格，再重试辞退，避免 `T004/T006` 这类“看起来已经空了却删不掉”的交易员继续卡住。
- 已补充并通过本轮验证：`python -m py_compile D:/qqokx/okx_quant/models.py D:/qqokx/okx_quant/okx_client.py D:/qqokx/okx_quant/engine.py D:/qqokx/okx_quant/notifications.py D:/qqokx/okx_quant/ui.py D:/qqokx/okx_quant/trader_desk_ui.py D:/qqokx/tests/test_strategy_engine.py D:/qqokx/tests/test_okx_client_orders.py D:/qqokx/tests/test_notifications.py D:/qqokx/tests/test_trader_desk_ui.py D:/qqokx/tests/test_ui.py`；`python -m unittest tests.test_strategy_engine tests.test_okx_client_orders tests.test_notifications tests.test_trader_desk_ui tests.test_ui`（134 项通过），以及针对交易员台按钮链路的 `python -m unittest tests.test_trader_desk_ui tests.test_ui`、`python -m unittest tests.test_trader_desk_ui`。

### v0.4.13 | 2026-04-26 | 详情面板防抢滚动与交易员平仓状态细化
- 已为 `D:/qqokx/okx_quant/trader_desk_ui.py` 的 `槽位记录与事件` 两个文本框加入“刷新时保留当前滚动位置”逻辑：自动刷新仍会更新内容，但当用户手动滚到中部查看 watcher、亏损单或事件日志时，不会再因为 `delete + insert` 被强行跳回顶部或底部。
- 已优化交易员槽位状态展示口径：`D:/qqokx/okx_quant/trader_desk_ui.py` 现会结合 `close_reason + net_pnl` 输出更直白的中文状态；例如 `策略主动平仓 + 净盈利` 显示为 `止盈净盈`，`策略主动平仓 + 净亏损` 显示为 `止盈净亏`，`止损原因` 显示为 `止损平仓`，避免把“平仓原因”和“最终盈亏结果”混在同一个 `亏损平仓/盈利平仓` 标签里。
- 已为主界面 `D:/qqokx/okx_quant/ui.py` 的 `选中策略详情` 面板加入“同一条会话自动刷新时保留滚动位置、切换到新会话时回到顶部”的细化逻辑；这样在观察同一条策略持续输出的详情时不会自动滚动抢焦点，但切换到另一条策略时仍然保持从头阅读的自然体验。
- 已补充并通过本轮验证：`python -m unittest tests.test_trader_desk_ui`（24 项通过）、`python -m unittest tests.test_ui`（75 项通过）。

### v0.4.14 | 2026-04-26 | 交易员总账与流水账本
- 已为 `D:/qqokx/okx_quant/trader_desk.py` 增加交易员账本统计层：新增 `TraderBookSummary`、`trader_realized_slots(...)`、`trader_book_summary(...)`，可基于交易员槽位平仓记录统一汇总总净盈亏、盈利/亏损/人工结束单数，以及盈利交易员/亏损交易员数量。
- 已为 `D:/qqokx/okx_quant/trader_desk.py` 的 `TraderSlotRecord` 增补 `bar`、`direction_label` 历史字段，并在 `D:/qqokx/okx_quant/ui.py` 创建交易员 watcher 槽位时同步固化当时的 `周期/方向`，避免后续修改交易规则后，历史账本流水被新配置覆盖显示。
- 已在 `D:/qqokx/okx_quant/trader_desk_ui.py` 的交易员管理台工具栏新增 `交易员账本` 按钮，并实现独立账本窗口：上半区 `交易员汇总` 按交易员统计平仓单数、胜率、累计净盈亏；下半区 `总账流水` 展示所有已平仓额度格的平仓时间、交易员、策略、周期、方向、会话、开平仓价、数量、净盈亏与原因，点选账本行还能联动定位回主交易员列表。
- 已补充并通过本轮验证：`python -m py_compile D:/qqokx/okx_quant/trader_desk.py D:/qqokx/okx_quant/trader_desk_ui.py D:/qqokx/okx_quant/ui.py D:/qqokx/tests/test_trader_desk.py D:/qqokx/tests/test_trader_desk_ui.py`；`python -m unittest tests.test_trader_desk tests.test_trader_desk_ui tests.test_ui`（113 项通过）。
### v0.4.15 | 2026-04-26 | 版本源头收正
- 已将版本源头统一更新为 `v0.4.15`：`D:/qqokx/pyproject.toml`、`D:/qqokx/okx_quant/__init__.py`、`D:/qqokx/README.md`、`D:/qqokx/软件开发指南.md`。
- `D:/qqokx/scripts/build_server_package.py` 读取的 `APP_VERSION` 现在会同步生成 `v0.4.15` 包名，避免继续产出 `v0.4.06`。
- 这一版是专门修正版本号不同步问题，不包含功能改动。

### v0.4.15 | 2026-04-26 | 普通量化策略总账本补充交易员列与组合筛选
- 已为 `D:/qqokx/okx_quant/ui.py` 的 `策略总账本` 窗口补充 `交易员` 列，并在 `策略汇总`、`账本流水` 两张表中同步显示 `交易员 / 状态` 字段，便于区分历史归因记录是否带有交易员来源信息。
- 已在同一窗口顶部新增筛选条，支持按 `API / 交易员 / 策略 / 标的 / 周期 / 方向 / 状态` 进行组合筛选，并提供 `重置筛选` 按钮；筛选后顶部汇总、分组汇总和流水账本会保持同一统计口径。
- 已为普通量化账本补充筛选与列位回归测试：`D:/qqokx/tests/test_ui.py` 新增对 `交易员/状态` 选项生成、组合筛选结果及新列位索引的断言校验。
- 已补充并通过本轮验证：`python -m py_compile D:/qqokx/okx_quant/ui.py D:/qqokx/tests/test_ui.py`；`python -m unittest tests.test_ui`（81 项通过）。

### v0.4.15 | 2026-04-27 | 交易员槽位状态文案收直白
- 已将 `D:/qqokx/okx_quant/trader_desk_ui.py` 中交易员槽位状态文案收口为更直白的中文：`watching` 现显示为 `等待开仓`，`stopped` 现显示为 `观察结束（未开仓）`，避免与交易员整条规则的总运行状态 `已停止` 混淆。
- 已同步更新交易员详情摘要中的阶段说明文案，并加宽槽位表 `状态` 列，减少新文案在表格中被截断的情况。
- 已补充并通过本轮验证：`python -m py_compile D:/qqokx/okx_quant/trader_desk_ui.py D:/qqokx/tests/test_trader_desk_ui.py`；`python -m unittest tests.test_trader_desk_ui`（27 项通过）。

### v0.4.16 | 2026-04-28 | 交易员会话串账、重复 watcher 与停止双弹窗修复
- 已修复 `D:/qqokx/okx_quant/ui.py` 中交易员槽位归因错误绑定的问题：交易员平仓/异常结束/开仓同步不再只按 `session_id` 查槽位，现优先按唯一的 `trader_slot_id` 绑定，解决重启后 `S02` 等会话号复用导致新一轮平仓结果串写进旧槽位、出现“开仓价/成本与底层账本不一致”的问题。
- 已增加交易员旧快照自修复：程序读取 `D:/qqokx_data/state/trader_desk.json` 时，会用 `strategy_trade_ledger` 按 `history_record_id` 回填已平仓槽位的开仓时间、开平仓价、数量、净盈亏与原因，自动纠正历史错位记录。
- 已修复会话编号重启复用问题：`D:/qqokx/okx_quant/ui.py` 现在会从历史策略记录、交易员槽位中读取已使用过的最大 `Sxx` 编号，后续新会话继续递增，不再每次重启都从 `S01/S02` 重新编号。
- 已修复交易员重复补 watcher 问题：在 `D:/qqokx/okx_quant/ui.py` 的 `_trader_desk_start_slot(...)` 增加“启动中 watcher 锁”，同一名交易员在 watcher 还没正式登记进槽位前，不会再次补启动第二条 watcher，解决 `T004` 的 `S63/S64`、`T003` 的 `S67/S68` 这类同一秒出现两条 `等待开仓` 的问题。
- 已修复交易员相关操作的停止双弹窗：交易员台调用底层停止时已经传 `show_dialog=False`，现在异步停止清理完成后会尊重这个标记，不再额外弹出第二个 `停止结果 / 停止提醒` 对话框；主界面手动停止单条普通策略仍保留原有弹窗行为。
- 已补充并通过本轮验证：`python -m py_compile D:/qqokx/okx_quant/ui.py D:/qqokx/tests/test_ui.py`；`python -m unittest tests.test_ui`（108 项通过）。



### v0.4.15 | 2026-04-26 | 下单方向模式按安全口径收口
- 已将 `D:/qqokx/okx_quant/ui.py` 的 `下单方向模式` 收口为“默认保守”规则：只有 `交易并下单 + 按交易标的价格（本地）` 时才允许选择 `固定买入 / 固定卖出`；在 `OKX 托管（仅同标的永续）`、`按自定义标的价格（本地）`、`只发信号邮件`、`EMA5/EMA8` 等不支持的模式下，控件会自动锁定为 `跟随信号`。
- 已在同一处补充联动提示文案：当模式不支持固定方向时，会直接显示原因说明，例如 `OKX 托管模式当前只支持跟随信号；如需固定买入/固定卖出，请切到按交易标的价格（本地）`，避免用户误以为“界面能选就一定能逆信号下单”。
- 已在 `D:/qqokx/okx_quant/engine.py` 增加运行前硬校验：即使旧模板导入、历史配置恢复或异常路径把 `fixed_buy/fixed_sell` 带入不支持的模式，引擎也会在启动前直接拦截，不再允许带风险配置继续实盘运行。
- 已补充并通过本轮验证：`python -m py_compile D:/qqokx/okx_quant/engine.py D:/qqokx/okx_quant/ui.py D:/qqokx/tests/test_strategy_engine.py D:/qqokx/tests/test_ui.py`、`python -m unittest D:/qqokx/tests/test_strategy_engine.py D:/qqokx/tests/test_ui.py`（138 项通过）。

### v0.4.15 | 2026-04-26 | 风险金输入卡顿修复
- 已修复 `D:/qqokx/okx_quant/ui.py` 中“风险金/固定数量提示”导致的主线程卡顿：此前每次修改 `风险金` 或 `固定数量` 时，提示刷新会在 UI 线程里兜底调用 `get_instrument(...)`，当本地缺少合约缓存时会同步请求 OKX，造成输入框短暂无响应。
- 现已改为“输入时只走本地缓存，缺缓存时后台异步补一次”：新增固定数量提示专用合约缓存与异步拉取队列，`风险金`、`固定数量` 的实时输入不再阻塞 Tk 主线程；合约补齐后会自动刷新提示文案。
- 已补充 `D:/qqokx/tests/test_ui.py` 回归测试，覆盖“优先命中本地缓存”“缺缓存时只触发异步补取、不做同步网络请求”两条关键路径。
- 已补充并通过本轮验证：`python -m py_compile D:/qqokx/okx_quant/ui.py D:/qqokx/tests/test_ui.py`、`python -m unittest D:/qqokx/tests/test_ui.py`（89 项通过）。

### v0.4.15 | 2026-04-26 | 启动参数提示增强与启动崩溃修复
- 已修复 `D:/qqokx/okx_quant/ui.py` 启动期 `NameError: STRATEGY_CROSS_ID is not defined` 导致主程序打不开的问题；缺失的策略常量导入已补齐，并完成 `QuantApp()` 创建与 `mainloop()` 进入/退出的实际冒烟验证。
- 已重写主界面启动确认弹窗文案：不再只罗列参数名和值，而是改为“基础信息 / 执行口径 / 参数说明 / 策略规则”结构，并为 `信号方向`、`下单方向模式`、`止盈止损模式`、`触发价格类型`、`止损ATR倍数`、`止盈ATR倍数`、`风险金`、`固定数量`、`启动追单窗口` 等关键项补充作用说明。
- 已在 `D:/qqokx/okx_quant/ui.py` 主界面表单里新增多组实时就地提示：`参数速记`（止损ATR、止盈ATR、每波最多开仓次数、启动追单窗口）、`趋势参数`（EMA小/中/大周期、挂单参考EMA）、`动态保护`（2R保本、手续费偏移、时间保本），参数调整时会同步刷新，不必等到确认弹窗才理解当前配置。
- 已补充 `D:/qqokx/tests/test_ui.py` 回归测试，覆盖启动确认文案解释、参数速记、趋势参数提示、动态保护提示等关键文本生成路径。
- 已补充并通过本轮验证：`python -m py_compile D:/qqokx/okx_quant/ui.py D:/qqokx/tests/test_ui.py`、`python -m unittest D:/qqokx/tests/test_ui.py`（93 项通过），以及实际启动验证：成功创建并关闭 `QuantApp`。
- 主界面运行中策略区新增发邮件功能两级开关：支持全局“发邮件：开/关”、运行中单会话“开启邮件/关闭邮件”，并接入运行时 notifier 判断；运行中策略表新增“邮件”列，选中策略详情新增发邮件状态显示。
- 主界面运行中策略区邮件列支持双击直接切换当前会话发邮件开关，列标题更新为“邮件(双击切换)”。
- 主界面运行中策略区发邮件功能补充为两级开关：支持全局“发邮件：开/关”、运行中单会话“开启邮件/关闭邮件”，运行中策略表新增“邮件”列，选中策略详情新增发邮件状态显示，并接入运行时 notifier 判断。
- 主界面运行中策略区邮件列支持双击直接切换当前会话发邮件开关，列标题更新为“邮件(双击切换)”。
- 主界面点击右上角关闭按钮或菜单“系统 -> 退出”时，新增二次确认弹窗；点“是”才关闭。
- 主界面运行中策略表支持双击快捷操作：会话=打开独立日志，交易员=打开并定位交易员管理台，邮件=切换发邮件开关，标的=打开实时K线图；相关表头增加悬停提示与主界面固定快捷操作说明。
- 历史策略表支持双击快捷操作：会话=打开独立日志，标的=打开对应会话实时K线图（会话仍存在时）。
- 普通量化策略总账本支持双击快捷操作：策略汇总表/账本流水表的交易员=打开并定位交易员管理台，标的=打开实时K线图；账本流水表的会话=打开独立日志；相关表头增加悬停提示。
- 信号观察台右侧运行中 signal_only 会话表支持双击快捷操作：会话=打开独立日志，标的=打开实时K线图；相关表头增加悬停提示。
### v0.4.16 | 2026-04-28 | 永续合约面值(ctVal)风险换算修复与张数折合日志
- 已修复 `D:/qqokx/okx_quant/engine.py` 中风险金下单数量计算未考虑合约面值的问题：`BTC/ETH/BNB/DOGE/SOL` 等 `USDT/USDC` 线性 `SWAP/FUTURES` 现按 `单张风险 = abs(开仓价-止损价) * ctVal * ctMult` 计算，再结合 `lotSz/minSz` 向下取整与最小下单量校验，不再把不同面值合约误当成相同币数量处理。
- 已同步修正实盘成交通知中的单笔盈亏口径：`FilledPosition` 新增并透传 `price_delta_multiplier`，`D:/qqokx/okx_quant/engine.py` 的平仓盈亏提示现按合约面值换算，避免下单数量与后续盈亏提示口径不一致。
- 已为实盘主链路日志补充“张数 + 折合币数”展示：`准备挂单`、`准备市价单`、`本地下单`、`挂单已成交`、`开仓成交`、`平仓成交`、`剩余数量`、`固定数量` 摘要等位置，现统一显示为例如 `1.63张（折合0.0163 BTC）`，便于交易员直接审核真实持仓规模。
- 已补充并通过本轮验证：`python -m py_compile D:/qqokx/okx_quant/engine.py D:/qqokx/tests/test_strategy_engine.py`、`python -m unittest D:/qqokx/tests/test_strategy_engine.py`（55 项通过），新增覆盖风险金按 `ctVal` 换算、无面值时保持原口径、以及 `1.63张（折合0.0163 BTC）` 日志格式的回归测试。

### v0.4.16 | 2026-04-28 | 持仓区新增平仓选中按钮
- 已在主界面持仓操作区与 `持仓大窗` 工具栏新增 `平仓选中` 按钮：位置分别在 `D:/qqokx/okx_quant/ui.py` 的主持仓按钮行与大窗按钮行，允许直接对当前选中的具体持仓发起平仓，不必再回到交易员管理台操作。
- 已为 `D:/qqokx/okx_quant/ui.py` 新增选中持仓人工平仓链路：点击后会先弹出方式选择框，支持 `市价平仓` 与 `挂买一/卖一平仓` 两种模式；其中 `平空` 会按买一挂单，`平多` 会按卖一挂单，并在提交后立即刷新持仓与委托视图。
- 已收口 API 上下文与安全限制：该功能只对“当前选中的具体持仓”生效，分组行不会误下单；同时优先使用当前持仓列表所属的 API 凭证与环境发单，避免误用别的账户上下文执行平仓。
- 已补充并通过本轮验证：`python -m py_compile D:/qqokx/okx_quant/ui.py D:/qqokx/tests/test_ui.py`、`python -m unittest tests.test_ui tests.test_trader_desk_ui`（153 项通过）；新增覆盖 `平多挂卖一` 价格方向与按钮提交流程的回归测试。

### v0.4.16 | 2026-04-28 | 交易员界面手动平仓方式升级
- 已为 `D:/qqokx/okx_quant/trader_desk_ui.py` 的 `手动平仓` 增加方式选择弹窗：支持 `市价平仓` 与 `挂买一/卖一平仓` 两种模式；其中 `平空` 会按买一挂单，`平多` 会按卖一挂单，取消则不执行。
- 已在 `D:/qqokx/okx_quant/ui.py` 收口交易员手动平仓执行链路：`flatten_trader_draft(...)` 与 `_submit_trader_manual_flatten_orders(...)` 现会透传平仓模式，并把方式写入交易员事件日志，便于后续核对“这次是市价平的，还是按最优价挂的”。
- 已为交易员槽位增加人工平仓待成交状态字段：`D:/qqokx/okx_quant/trader_desk.py` 的 `TraderSlotRecord` 新增待成交平仓单号、客户端单号、模式、合约等字段；当选择 `挂买一/卖一平仓` 时，如订单尚未成交，槽位会保持 `open`，额度不会提前释放。
- 已在 `D:/qqokx/okx_quant/ui.py` 增加待成交人工平仓回查：后台会持续查询这类平仓单状态，只有订单真实成交后才把槽位标记为 `closed_manual` 并释放额度；若订单仍 `live`、被撤销或查询失败，也会保留真实持仓并写事件日志，避免本地账本与交易所状态对不上。
- 已修复交易员手动平仓与 watcher 停止的串扰：当策略线程已停、但人工最优价平仓单仍在待成交时，系统不再误把槽位直接记成“已手动平仓”，而是保留为待成交并继续回查。
- 已补充并通过本轮验证：`python -m py_compile D:/qqokx/okx_quant/trader_desk.py D:/qqokx/okx_quant/trader_desk_ui.py D:/qqokx/okx_quant/ui.py D:/qqokx/tests/test_ui.py D:/qqokx/tests/test_trader_desk_ui.py`、`python -m unittest tests.test_ui tests.test_trader_desk_ui`（150 项通过）；新增覆盖交易员最优价平仓“待成交不提前释放额度”“成交后再结槽位”的回归测试。
### v0.4.16 | 2026-04-29 | 启动区最小下单门槛与最低风险金提示
- 已在 `D:/qqokx/okx_quant/ui.py` 的启动参数区为 `固定数量` 提示下方新增 `下单门槛` 动态提示：会联动当前 `信号标的 / 下单标的 / 策略 / K线周期 / EMA / ATR / 止损ATR倍数 / 风险金 / 止盈止损模式 / 运行模式`，直接显示当前标的的最小下单量、折合币数，以及按当前止损估算的最低风险金门槛。
- 已为永续/期货合约补充更直白的最小下单提示文案：例如 `1张（折合0.01 BNB）`，并在风险金不足时明确提示“当前填写还不够下最小一笔”；若当前尚无有效信号、止盈止损参考的是其他标的、或当前模式本身不下单，也会给出对应说明而不是静默失败。
- 已补充 `D:/qqokx/tests/test_ui.py` 回归测试，覆盖最小下单量折算展示、最低风险金不足提示、以及缺少合约信息时的加载提示；同时顺手清理了 `D:/qqokx/okx_quant/ui.py` 中测试邮件相关的可见乱码占位文案。
- 已补充并通过本轮验证：`python -m py_compile D:/qqokx/okx_quant/ui.py D:/qqokx/tests/test_ui.py`、`python -m unittest D:/qqokx/tests/test_ui.py`（126 项通过）。
- 主界面/邮件：修复普通成交邮件未带 API 配置名的问题；补齐策略启动常用路径创建 Credentials 时的 profile_name，并给 StrategyEngine 增加 api_name 双保险透传，覆盖普通开仓成交、动态委托成交、部分成交、本地下单成交、本地止盈止损平仓成交等成交类邮件。
