# 发版待打包清单

## 当前待打包

- 暂无

## 已收录

### v0.3.94 | 2026-04-16 | 动态止盈默认值与手续费偏移开关

- 已新增动态止盈 `手续费偏移` 开关，可独立控制动态止盈触发位和止损上移位是否按 `2倍Taker手续费` 留缓冲。
- 已将动态策略默认值调整为：`动态止盈`、`止损 1.5ATR`、`2R保本开启`、`手续费偏移开启`、`每波最多开仓 1 次`、`挂单参考 EMA55`。
- 已在主界面、回测窗口、回测报告、参数摘要、导出报告、放大图头部和本地止盈止损日志中补充 `手续费偏移` 状态展示。
- 已补充并通过动态止盈相关测试：`python -m unittest D:\qqokx\tests\test_backtest.py D:\qqokx\tests\test_strategy_engine.py`。
- 已新增动态止盈专题分析脚本，并整理保留当前汇报摘要、口头提纲、实盘开关总表和答问材料。

### v0.3.93 | 2026-04-16 | 动态止盈 2R 保本

- 已新增动态止盈 `启用2R保本` 开关：开启时 `2R -> 开仓价+2倍Taker手续费`，关闭时 `2R -> 1R+2倍Taker手续费`，`3R` 起统一按 `n-1R+2倍Taker手续费` 递推，做空方向镜像处理。
- 已同步回测与本地止盈止损引擎的动态止盈逻辑，统一按 `2倍Taker手续费` 计算动态触发价与动态止损位，避免回测与实盘辅助规则不一致。
- 已在主界面和回测窗口补充 `启用2R保本` 勾选项，并在非动态止盈模式下自动禁用，避免误配置。
- 已在回测报告、参数摘要、导出报告和放大图头部补充 `2R保本` 信息，便于核对当前策略设置。
- 已补充并通过动态止盈相关测试：`python -m unittest D:\qqokx\tests\test_backtest.py D:\qqokx\tests\test_strategy_engine.py`

### v0.3.92 | 2026-04-15 | 回测-图表与报告UI

- 已补充回测图表大窗头部策略信息，显示编号、策略、交易对、周期、方向、挂单 EMA、指标参数、止损、止盈方式、每波开仓次数、交易数、胜率、总盈亏、最大回撤。
- 已实现放大图在切换回测记录、清空记录、回测报错时自动刷新头部信息，避免残留旧策略信息。
- 已修复单组回测进入“矩阵对比”页显示问号的问题，改为展示单组摘要卡。
- 已补充单组回测“矩阵对比”页的关键策略标签块，明确显示挂单 EMA、止盈模式、每波开仓次数。
- 已补充单组回测“参数热力图”页提示文案，明确说明热力图仅在批量参数对比时生成。
