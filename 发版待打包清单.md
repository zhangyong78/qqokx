# 发版待打包清单
## 当前待打包
- 暂无

## 已收录
### v0.3.100 | 2026-04-18 | 现货增强三十六计主界面接入、持仓保护降噪与标准R倍数修正
- 已将 `现货增强三十六计` 作为 live-only 策略接入主界面启动链路，并将回测窗口候选策略与实盘候选策略分离；选中该策略时打开回测会自动回退到可回测策略，避免启动配置错位。
- 已新增 `D:/qqokx/okx_quant/enhanced_live_engine.py`：支持现货信号标的与永续交易标的自动换算、信号源 K 线加载、人工池优先减仓与会话产物落盘，为主界面直接启动“现货增强三十六计”提供执行引擎。
- 已在 `D:/qqokx/okx_quant/ui.py` 与 `D:/qqokx/okx_quant/strategy_catalog.py` 补齐“现货增强三十六计”参数校验与默认值：`固定数量` 作为单槽数量，`每波最多开仓次数` 作为每个方向最大槽位数，并收口为“交易并下单”模式。
- 已为 `D:/qqokx/okx_quant/position_protection.py` 增加监控心跳节流：连续“监控中”状态默认每 `30s` 才写一次日志，但状态消息仍实时刷新；若刚经历网络异常等非监控状态，恢复监控后会立即补打一条心跳，减少持仓保护日志刷屏。
- 已修复回测标准 `R` 倍数口径：`D:/qqokx/okx_quant/backtest.py` 新增 `_position_initial_risk_value`，未平仓展示、人工池持仓和已平仓交易统一按开仓价到初始止损的原始风险计算，不再因止损上移而放大 `R` 值。
- 已补充 `D:/qqokx/tests/test_backtest.py`、`D:/qqokx/tests/test_enhanced_live_engine.py` 与 `D:/qqokx/tests/test_position_protection.py` 回归覆盖，并完成本轮验证：`python -m unittest tests.test_backtest tests.test_enhanced_live_engine tests.test_position_protection`（106 项测试通过）与 `python -m py_compile okx_quant\\backtest.py okx_quant\\strategy_catalog.py okx_quant\\ui.py okx_quant\\enhanced_live_engine.py okx_quant\\position_protection.py tests\\test_backtest.py tests\\test_enhanced_live_engine.py tests\\test_position_protection.py`。

### v0.3.99 | 2026-04-17 | 历史策略溯源、独立日志与清理能力
- 已新增历史策略持久化文件 `D:/qqokx/.okx_quant_strategy_history.json`：策略启动、手动停止、线程自然结束、程序关闭与异常退出恢复都会同步落盘，记录会话号、参数快照、状态、结束原因与独立日志路径，方便追溯。
- 已新增“历史策略”窗口查看链路：支持历史列表、详情查看、打开日志、复制日志路径，便于直接定位某次实盘会话的执行记录。
- 已为每次策略会话补齐独立日志文件，统一写入 `D:/qqokx/logs/strategy_sessions/`；总日志与会话独立日志会同时保留，历史记录中会同步保存 `log_file_path`。
- 已在“运行中策略”区域新增 `清空已停止` 按钮，并补充二次确认、清空后自动选中剩余会话逻辑；该操作只清主界面列表，不删历史策略记录和独立日志。
- 已在“历史策略”窗口新增 `删除选中` 与 `清空历史`；默认只删除历史记录，不删除独立日志；仍对应运行中或停止中会话的记录会自动保留，避免被后台线程再次写回。
- 已完成本轮验证：`python -m py_compile D:/qqokx/okx_quant/ui.py D:/qqokx/okx_quant/persistence.py D:/qqokx/okx_quant/log_utils.py D:/qqokx/tests/test_persistence.py D:/qqokx/tests/test_log_utils.py D:/qqokx/tests/test_ui.py`、`python -m unittest D:/qqokx/tests/test_persistence.py D:/qqokx/tests/test_log_utils.py D:/qqokx/tests/test_ui.py`（30 项测试通过），以及历史策略窗口/主窗口 UI smoke 验证。

### v0.3.98 | 2026-04-17 | 1H 无信号阶段降噪与空窗降频
- 已对 `EMA 动态委托` 实盘循环补充“同一根已确认 K 线仅记录一次无信号日志”控制，避免 `1H` 策略在同一根 K 线下按轮询秒数重复输出“当前无法生成挂单 / 当前无法生成动态开仓价”。
- 已为无活动挂单、无本地触发条件的空窗阶段增加保守降频：等待时长会结合 `K` 线周期、下一根收线前剩余时间与 `60s` 上限自动计算，减少无意义的高频重算，同时不影响临近新收线时的重新评估。
- 已保留执行态高频链路：存在活动挂单、本地触发条件或动态止损上移监控时，仍继续按原 `poll_seconds` 频率执行，不降低成交确认、撤单同步与 OKX 止损上移响应。
- 已补充并通过本轮回归：`python -m py_compile D:\qqokx\okx_quant\engine.py D:\qqokx\tests\test_strategy_engine.py` 与 `python -m unittest D:\qqokx\tests\test_strategy_engine.py`，新增覆盖空窗降频与“同 K 线只记一次日志”的测试场景。

### v0.3.97 | 2026-04-17 | 增强回测实验室、委托视图与 OKX 容错加固
- 已统一补强实盘策略的 OKX 容错：标的信息、K 线、订单状态、挂单列表、持仓列表、触发价、ATR 快照与成交价估算统一接入受控重试；瞬时网络异常会自动重试，业务拒绝与参数错误仍直接抛出。
- 已补齐策略引擎、智能下单与持仓保护的写入恢复链路：下单、撤单、平仓与算法止损改单在响应丢失时会先按 `clOrdId / algoClOrdId` 回查，必要时使用同一 `clOrdId` 补发一次，避免任务被瞬时网络抖动直接打断。
- 已继续加固 `动态止盈 + OKX 托管`：补齐 ASCII 安全 `clOrdId`、候选止损有效性判断、按 `algoClOrdId` 定位算法委托与动态上移止损恢复，`ETH-USDT-SWAP` 模拟盘整链路已完成复核且无残留挂单、无残留持仓。
- 已将 `账户信息` 窗口扩展为 `资产明细 / 当前委托 / 历史委托` 标签页结构，并新增 `刷新全部` 入口；用户不必再切换到持仓大窗也能查看、刷新和撤销账户委托。
- 已将当前委托/历史委托表格、筛选、详情与撤单链路抽为可复用组件，`账户信息` 与 `持仓大窗` 共用同一套数据源和列定义；多窗口并行显示时，刷新后会同步更新两处视图且不会继续引用已关闭窗口的失效控件。
- 已补齐智能下单在 `long_short` 模式下的 `posSide` 处理：网格单、条件触发单、止盈止损平仓和激进成交路径都会按多空方向带出正确仓位侧，并补充掉单回查与补发测试。
- 已落地现货增强三十六计回测实验室底座：新增子策略注册、信号聚合、门控规则、额度引擎、运行时配置热切换、样例策略包、分段历史加载与增强回测执行链路，便于持续迭代现货增强策略。
- 已补齐持仓与证据审计导出：新增 `position_ledger.csv`、`position_ledger_summary.csv`、`manual_review_manifest.csv`、`evidence_index.csv`、`evidences.json`、证据 K 线小图、人工接管画廊、`position_management.html`、`total_position_management.html` 与 `total_position_summary.csv`，并支持多种总仓合并口径与价格域隔离。
- 已新增增强实验室报告脚本 `scripts/generate_enhanced_seed_lab_smoke_report.py` 与 `scripts/generate_enhanced_seed_lab_realdata_report.py`，便于生成冒烟报告与真实样例审计材料。
- 已补充并通过本轮核心回归：`python -m unittest tests.test_strategy_engine tests.test_smart_order tests.test_okx_client_orders tests.test_enhanced_strategy_foundation tests.test_enhanced_strategy_lab`，并完成 `python -m py_compile` 语法校验。

### v0.3.96 | 2026-04-17 | 实盘标的收口与 OKX 动态止损联动
- 实盘启动区已统一为单一`交易标的`心智：移除单独的`下单标的`选择，运行中列表与启动确认文案同步收口；内部仍保留`inst_id / trade_inst_id`兼容位，便于后续扩展到期权或跨标的场景。
- 实盘标的输入已全部收紧为 OKX 正式永续格式与下拉选择：默认值改为`ETH-USDT-SWAP`，`信号/下单/自定义触发标的`均改为只读列表；`加载 OKX SWAP`后会同步刷新候选项。
- 实盘默认参数已调整为更保守的起始值：`风险金=10U`、`止损 ATR=2`，并同步修正 EMA5/8 分支的界面默认值与启动时内部兜底值，避免界面与真实执行不一致。
- 已打通`动态止盈 + OKX 托管`执行链路：动态止盈模式下开仓时仅向 OKX 附加初始止损，不再预挂固定止盈；成交后策略持续监控价格，并按现有`2R保本 / 手续费偏移 / nR 锁定 (n-1)R`规则动态调用 OKX 算法单改单接口上移止损。
- 已补充 OKX 算法止损改单能力与日志链路：为挂单附加独立`algoClOrdId`，新增`amend-algos`封装、持仓存活检测和动态止损上移日志；同时移除 UI 中“动态止盈不能选 OKX 托管”的限制。
- 已修复策略引擎`clOrdId`生成问题：原先会从中文策略名中带入非 ASCII 字符，导致 OKX 拒单并报“操作全部失败”；现已收紧为仅生成 ASCII 字母数字订单号，避免 UI 启动策略时出现隐性拒单。
- 已修复 OKX 动态止损上移竞态：成交后先按`algoClOrdId`确认算法止损委托已进入挂单列表，再携带`algoId + algoClOrdId`执行`amend-algos`；若交易所侧委托尚未就绪，会自动重试而不是直接停掉策略。
- 已完成`ETH-USDT-SWAP`模拟盘 UI 级整链路验证：从主界面`启动`按钮创建策略会话，到自动提交挂单、成交、附加 OKX 止损、动态上移止损，整条链路已实测跑通；验证结束后已确认无残留持仓、无残留挂单。
- 已补充并通过本轮回归：`python -m unittest D:\qqokx\tests\test_strategy_engine.py D:\qqokx\tests\test_okx_client_orders.py`，覆盖`clOrdId` ASCII 限制、按`algoClOrdId`定位算法委托与 OKX 止损改单请求格式。
