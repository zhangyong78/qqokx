# OKX 策略工作台

当前版本：`v0.3.88`

这是一个面向 OKX 的桌面交易工作台，围绕“监控、交易、保护、回测、分析”构建。当前项目已经包含：

- 策略运行与多会话管理
- 账户持仓、历史成交、历史仓位
- 期权持仓保护与回放模拟
- 多币种信号监控与邮件通知
- Deribit 波动率指数查看与波动率监控
- 回测、矩阵对比、历史报告持久化
- 无限下单 / 条件单 / 网格任务
- 期权策略计算器与图表分析

## 1. 主界面与策略工作台

主界面支持同时运行多个策略会话，当前已接入：

- `EMA 动态委托-多头`
- `EMA 动态委托-空头`
- `EMA 穿越市价`
- `4H EMA5 / EMA8 金叉死叉`

主界面能力包括：

- 多策略同时运行
- 会话列表查看与停止
- 多 API 账户切换
- 策略会话与运行日志显示 `API配置`
- 账户持仓区优先显示
- 主界面不再显示选中持仓详情，重点聚焦策略与持仓
- 期权保护窗口会锁定当前选中的期权，不会被自动刷新改回第一条持仓

## 2. 账户持仓与历史数据

主界面和持仓大窗提供接近 OKX Windows 客户端的持仓视图，支持：

- 当前持仓树形分组
- 风险单元 / 到期日分组
- 历史成交
- 历史仓位
- 账户信息弹窗
- 自定义列显示
- 搜索、筛选、摘要统计

当前持仓与持仓大窗的增强点：

- 分组行支持显示 `持仓个数 + 持仓数量汇总`
- 摘要区支持显示：
  - `买购数量`
  - `卖购数量`
  - `买沽数量`
  - `卖沽数量`
- `标记价 / 标记≈USDT`
- `开仓价 / 开仓≈USDT`
- `时间价值 / 时间≈USDT`
- `内在价值 / 内在≈USDT`
- `买一价 / 买一≈USDT`
- `卖一价 / 卖一≈USDT`
- `浮盈亏 / 浮盈≈USDT`
- `Theta / Theta≈USDT`
- 大窗里 `≈USDT` 列宽已做紧凑化处理
- `买一 / 卖一 / 买一≈USDT / 卖一≈USDT` 默认放在 `标记价` 前面，并使用更紧凑的列宽

历史成交当前支持：

- 默认读取 `500` 条
- 单独 `刷新历史成交`
- `增加100条`，之后每次 `增加200条`
- 按 `类型 / 方向 / 搜索` 筛选
- `带入合约`
- `带入到期前缀`
- 补充到期被行权 / 交割类记录

显示口径：

- 行权 / 交割成交价按 `2` 位小数显示
- `已实现盈亏` 按交易币种精度显示

## 3. 期权持仓保护

支持针对已有期权仓位设置本地止盈止损保护。

支持方式：

- 按期权标记价格触发
- 按现货价格触发
- 设定价格报单
- 标记价格加减滑点报单

内置风控：

- 网络异常持续重试
- 3 分钟限频异常邮件
- 防重复平仓
- 反向仓位熔断
- 价格异常拦截
- 触发后锁定为待平仓，不回退到普通监控
- IOC 未成交撤单后自动进入平仓重试
- 多次未成交后进入 `持续未成交待人工`
- 成交日志与状态文案已统一为正常中文
- 保护任务列表、详情、日志与邮件会显示 `API配置`

价格拦截会参考：

- 现货价格
- 行权价
- 开仓均价
- bid / ask / last

如果报单价格明显异常：

- 不自动报单
- 任务进入异常
- 发邮件提醒
- 等待人工处理

平仓重试策略：

- 一旦止盈 / 止损触发，就进入 `已触发待平仓`
- 如果平仓单 `canceled` 且未成交，不直接记异常
- 系统会继续取最新盘口并重新尝试平仓
- 同一保护任务在相同未成交状态下不会重复发送邮件，避免撤单重报时刷屏
- 只有状态发生变化（如首次进入重试、转为 `持续未成交待人工`、最终成交、真正异常）才会再次发邮件
- 只有真正的业务错误、价格保护拦截或反向仓位等情况才会进入 `异常`
- 所有日志统一带时间前缀，当前采用更紧凑的 `[MM-DD HH:MM:SS]` 格式，便于排查成交与保护时序
- 任务详情区会记录触发标的、触发价格类型、止盈/止损报单方式、设定价格、滑点和轮询秒数，便于回看配置

## 4. 多币种信号监控

支持按收线后检测信号，不做无意义秒级轮询。

当前支持的信号包括：

- `EMA21/55 金叉死叉`
- `EMA55 斜率改变`
- `EMA55 突破`
- `K线形态信号`

监控窗口支持：

- 监控任务
- 最近触发信号
- 监控日志
- 实时诊断
- 整页纵向滚动，适应不同分辨率
- 历史信号图预览 / 放大查看

通知方式：

- 邮件通知
- 实时诊断输出

## 5. Deribit 波动率工具

项目内包含两块 Deribit 波动率能力。

### 5.1 波动率指数窗口

支持：

- `BTC / ETH`
- `1小时 / 4小时 / 1天`
- 默认查看 `300` 根 K 线
- 波动率 K 线与同币种同周期现货 K 线双图联动显示
- 支持 `平均K线` 切换
- 鼠标滚轮缩放
- 左键拖动
- 双击重置视图
- 双图十字光标联动
- 页面打开后自动加载历史数据
- 切换币种 / 周期 / K线数量后自动刷新图表
- `日线对齐` 支持：
  - `北京时间凌晨12点`
  - `北京时间早上8点`

说明：

- Deribit 官方接口没有原生 `4小时 / 本地对齐日线`
- 程序会以 `1小时` 为底层数据源，本地聚合成：
  - `4小时`
  - `1天`
- `1天` 会先按 `4小时` 对齐，再聚合成日线
- 程序会从 `2021年` 开始建立波动率与现货 `1小时` 全量本地底仓
- 打开页面时会先读取本地缓存，再后台补最新小时数据
- 切换到 `4小时 / 1天` 时，不再重新整段拉接口，而是直接基于本地 `1小时` 缓存聚合
- 默认打开时只显示最近 `300` 根，但缩小视图或向左拖动时，会继续露出本地缓存中的更早历史
- `1小时` 周期最大支持 `30000` 根输入，界面默认仍显示最近窗口
- 自动刷新会按“最后一根K线的下一根边界”排程，而不是简单固定等待 `1` 小时
- 运行日志会带上当前已加载数据的 `最后一根K线` 时间，方便判断是否已经更新到最新
- `日线对齐` 选择会记住上次设置
- 网络超时时会优先保留当前已显示图表和缓存数据，不再用英文弹窗反复打断查看

本地缓存与存储：

- 波动率缓存文件：
  - `.okx_quant_deribit_volatility_cache.json`
- 存储位置：
  - `D:\qqokx\.okx_quant_deribit_volatility_cache.json`
- 文件性质：
  - 本地持久缓存文件
  - 不是临时文件
- 内容包括：
  - `BTC / ETH` 的全量 `1小时` 波动率 K 线底仓
  - 对应同币种现货全量 `1小时` K 线底仓
  - 最近获取时间
  - `日线对齐` 的界面记忆状态
- 典型缓存 key：
  - `BTC|hourly_base`
  - `ETH|hourly_base`
- 典型字段：
  - `volatility_candles`
  - `spot_candles`
  - `fetched_at`
  - `spot_inst_id`

### 5.2 波动率监控

当前支持的明显信号包括：

- 连涨后大阴反转
- 连跌后大阳反转
- 窄幅向上突破
- 窄幅向下突破
- 箱体向上突破
- 箱体向下突破
- `EMA34` 转强
- `EMA34` 转弱

窗口支持：

- 监控任务
- 最近触发信号
- 监控日志
- 实时诊断
- 整页纵向滚动，适应不同分辨率

## 6. 回测系统

入口：

- `工具 -> 打开回测窗口`
- `工具 -> 打开回测对比总览`

当前支持周期：

- `5分钟`
- `15分钟`
- `1小时`
- `4小时`

回测能力包括：

- 单次最多 `10000` 根已收盘 K 线
- 本地历史 K 线缓存
- 自动补最新历史数据
- 支持按时间段回测
- 自动补足指标预热和前置 K 线
- 前 `200` 根仅用于预热，不参与交易
- K 线图支持缩放、拖动、十字光标、悬停详情
- 资金曲线 / 净值相关展示
- 当前报告
- 回测对比
- 矩阵对比
- 参数热力图
- 周期统计
- 历史回测持久化

实盘策略运行时会自动按当前指标需要补足回看 K 线：

- `EMA 动态委托-多头`
- `EMA 动态委托-空头`
- `EMA 穿越市价`
- 本地触发版
- 只监控信号版

动态委托策略说明：

- `EMA 动态委托` 已拆成两套独立策略：
  - `EMA 动态委托-多头`
  - `EMA 动态委托-空头`
- 两套策略分别独立运行、独立回测、独立调参
- 每套都支持：
  - `固定止盈`
  - `动态止盈`
  - `每波最多开仓次数`
- 新增 `挂单参考EMA` 参数：
  - 用来决定挂单价格参考哪一条 EMA
  - 填 `0` 表示跟随 `EMA小周期`
  - 也可以单独填成 `5 / 8 / 13 / 21 / 34` 等周期，做成“信号 EMA”和“委托 EMA”分离
- `每波最多开仓次数` 会按趋势波段独立计数，趋势失效后重新形成时自动清零
- `动态止盈` 采用永久阶梯规则：
  - 达到 `2R`：止损移动到开仓价加双向手续费
  - 达到 `3R`：止损移动到 `2R`
  - 达到 `4R`：止损移动到 `3R`
  - 之后持续按 `nR -> 锁定 (n-1)R` 规则推进
- 动态委托回测与实盘都按“每根新 K 线撤旧挂新，使用最新 EMA 重新挂委托”的方式运行

回测结果增强：

- `交易明细` 支持显示：
  - `序号`
  - `止损值`
  - `ATR值`
  - `开仓数量`
- 回测结束时如果还有未平仓头寸，会在当前报告里单独列出 `期末未平仓`

回测成本模型支持：

- Maker 手续费
- Taker 手续费
- 滑点
- 资金费率

ATR 批量矩阵规则：

- `止损 1 ATR -> 止盈 1 / 2 / 3 ATR`
- `止损 1.5 ATR -> 止盈 1.5 / 3 / 4.5 ATR`
- `止损 2 ATR -> 止盈 2 / 4 / 6 ATR`
- `固定止盈` 批量回测支持 `36` 组组合：
  - `SL x TP` 的 `3 x 3`
  - `每波最多开仓次数 = 0 / 1 / 2 / 3`
- `动态止盈` 批量回测支持 `12` 组组合：
  - `SL = 1 / 1.5 / 2 ATR`
  - `每波最多开仓次数 = 0 / 1 / 2 / 3`
- `矩阵对比` 与 `参数热力图` 会按当前止盈方式自动切换展示模式：
  - `固定止盈` 按开仓次数分层查看 `3 x 3`
  - `动态止盈` 按 `止损倍数 x 开仓次数` 显示 `3 x 4` 对比

## 7. 无限下单

入口：

- `工具 -> 打开无限下单`

这是一个单合约弹出式交易工作台。

特点：

- 一个窗口只服务一个合约
- 有活动任务时锁定当前合约
- 关闭窗口前必须停止任务并撤掉活动委托

### 7.1 手工下单

支持：

- 限价
- 最优价
- 市价（激进 IOC）
- IOC
- FOK
- Post Only

最优价逻辑：

- `buy` 挂 `买一`
- `sell` 挂 `卖一`

期权数量口径：

- 当前合约类型为 `OPTION` 时，数量按“币数”输入
- 程序会按合约面值自动换算成张数
- 仓位限制也按币数输入和显示，内部自动换算成张数

### 7.2 条件单

支持：

- 上穿 / 下穿触发
- 当前合约 / 自定义标的触发
- 触发后限价或激进 IOC

### 7.3 止盈止损

支持对当前合约单独启动本地止盈止损任务，并复用期权保护的严谨风控。

### 7.4 点击报价网格

这里不是传统铺多层静态网格，而是“点击报价后的往返挂单链”。

支持：

- 同一合约下多条网格任务并存
- 买成后按“多单参数”挂反向卖单
- 卖成后按“空单参数”挂反向买单
- 多单参数、空单参数可不同

### 7.5 规则盘口

支持：

- 显示交易所盘口买卖量
- 自定义价格筛选步长
- 当前价 / 买一 / 卖一高亮
- 点击价格带入
- 网格模式开启后，点击买卖列直接建网格任务

### 7.6 任务持久化

若软件异常关闭：

- 网格任务
- 条件单
- 止盈止损任务

会在下次打开时恢复显示，并允许再次启动。

## 8. 期权策略计算器

入口：

- `工具 -> 打开期权策略计算器`

支持：

- 读取期权链并显示认购 / 认沽报价
- 导入到期持仓
- 导入系列持仓
- 保存 / 加载 / 删除策略
- 组合公式线性表达式
- 到期盈亏图
- 组合 K 线
- 图表大窗里的 `波动率K线 / 叠加对比` 下图使用 `Deribit 波动率指数` 本地缓存
- 策略腿分组查看
- 图表大窗
- Greeks 汇总与组合摘要

### 8.1 策略腿

当前策略腿表格支持：

- 持仓价
- 标记价
- Delta
- Gamma
- Theta
- Vega

其中：

- `持仓价` 可手工修改，用于分析真实建仓成本
- `标记价` 用于当前市值与现价参考
- 刷新报价不会覆盖持仓价

### 8.2 组合 K 线

默认值：

- `K线周期 = 1H`
- `K线数量 = 1000`
- `图表币种 = 结算币`

特点：

- 组合 K 线采用 OKX 期权标记价格组合
- 支持缩放、拖动、十字光标
- 支持 `只影响组合K线`
- 支持 `消除影线`
  - 开启后只按 `open / close` 计算高低
- 颜色规则为：
  - `绿涨`
  - `红跌`

### 8.3 图表大窗

图表大窗包含：

- 到期盈亏图
- 组合K线
- 波动率K线
- 叠加对比

特点：

- 到期盈亏图支持 hover 跟随线
- 组合K线支持缩放、拖动、十字光标
- 波动率K线周期与组合K线一致
- `叠加对比` 改为上下双图
  - 上图：组合K线
  - 下图：波动率K线
- 上下双图共用时间轴
- 十字光标上下图联动

## 9. 期权展期建议

入口：

- 主界面持仓区选中单腿卖出期权后点击 `展期建议`
- 持仓大窗中选中单腿卖出期权后点击 `展期建议`

第一版定位：

- 面向 `short call / short put`
- 自动扫描后续到期可用于展期/移仓的候选合约
- 给出 `平旧 + 开新` 的建议排序
- 可一键 `送入期权策略计算器`

当前支持：

- 扫描条件：
  - `目标偏好`
  - `行权价方向`
  - `档位优先`
  - `候选数量`
- 行权价方向支持：
  - `更虚值优先`
  - `同行权价优先`
  - `同行权价及更安全方向`
  - `全部`
- 目标偏好支持：
  - `优先净收权利金`
  - `优先降低风险`
  - `优先保持 Delta 接近`
  - `优先时间价值`
  - `优先更近到期`
- 建议结果直接显示：
  - `买一价`
  - `卖一价`
  - `标记价`
  - `预计净收/付`
  - `新Delta`
  - `风险变化`
  - `价差`
- 没有 `买一价` 的候选合约会自动过滤，不纳入建议结果

## 10. 本地文件

运行过程中会在本地生成这些文件：

- `.okx_quant_credentials.json`：API 凭证
- `.okx_quant_settings.json`：程序设置
- `.okx_quant_backtest_history.json`：回测历史
- `.okx_quant_smart_order_tasks.json`：无限下单任务快照
- `.okx_quant_smart_order_favorites.json`：无限下单自选合约
- `.okx_quant_deribit_volatility_cache.json`：Deribit 波动率 `1小时` 底层缓存与日线对齐界面状态
- `.okx_quant_candle_cache/`：本地 K 线缓存
- `logs/YYYY-MM-DD.log`：正式日志文件，界面日志会同步落盘
- 运行日志时间前缀使用更紧凑的 `[MM-DD HH:MM:SS]`

更新服务器时，建议至少保留：

- `.okx_quant_credentials.json`
- `.okx_quant_settings.json`

如需无缝续用，再保留：

- `.okx_quant_backtest_history.json`
- `.okx_quant_smart_order_tasks.json`
- `.okx_quant_smart_order_favorites.json`
- `.okx_quant_candle_cache/`

## 11. 更新日志
当前版本：v0.3.88

### v0.3.88

- `EMA 动态委托-多头 / 空头` 新增 `挂单参考EMA` 参数
- 现在可以把“信号判断用的 EMA 小周期”和“实际挂单参考的 EMA 周期”拆开设置
- `挂单参考EMA = 0` 时，保持旧逻辑，自动跟随 `EMA小周期`
- 主界面、回测窗口、运行确认、策略详情、小时调试输出、回测报告、批量导出摘要都已同步显示该参数
- 实盘 / 本地策略 / 只监控信号 / 回测链路都已接入该参数，并按所选 EMA 周期补足指标预热 K 线

### v0.3.87

- `动态止盈` 的批量回测从原来的 `4` 组扩展为 `12` 组：
  - 行为 `SL = 1 / 1.5 / 2 ATR`
  - 列为 `每波最多开仓次数 = 0 / 1 / 2 / 3`
- `矩阵对比` 页面中，动态止盈现在改为 `3 x 4` 矩阵，不再只做单行横向比较
- `参数热力图` 页面中，动态止盈也同步切换为 `3 x 4` 结构
- 批量导出报告中，动态止盈部分改为按 `止损倍数 x 开仓次数` 输出
- 回测相关文档说明同步更新
### v0.3.86

- `EMA 动态委托-多头 / 空头` 的批量回测已按 `止盈方式` 区分：
  - `固定止盈`：回测 `SL 3 x TP 3 x 每波最多开仓次数(0/1/2/3)`，共 `36` 组
  - `动态止盈`：只比较 `每波最多开仓次数(0/1/2/3)`，共 `4` 组
- `矩阵对比` 页面改成自适应展示：
  - `固定止盈` 增加“开仓次数层”切片，逐层查看 `3 x 3` 矩阵
  - `动态止盈` 自动切成 `4` 组横向对比
- `参数热力图` 同步改成与批量模式一致的分层/四列展示
- 批量导出报告已同步适配：
  - `固定止盈` 按开仓次数分层导出
  - `动态止盈` 直接导出 `4` 组开仓次数对比

### v0.3.85

- `EMA 动态委托` 正式拆成 `多头` 与 `空头` 两套独立策略
- 两套策略都新增：
  - `止盈方式（固定止盈 / 动态止盈）`
  - `每波最多开仓次数`
- `动态止盈` 按永久阶梯规则运行：
  - `2R -> 保本加双向手续费`
  - `3R -> 锁 2R`
  - `4R -> 锁 3R`
  - 后续持续按 `nR -> 锁定 (n-1)R`
- 回测与实盘统一支持这套动态止盈与波段开仓次数控制

### v0.3.84

- 相对稳定版本，更新 `EMA` 动态止盈前的正常版本
提交号：3bc446f
已推送到 origin/main
这次一起入库的重点：

Deribit 波动率指数 切换币种/周期、点击 立即刷新 时，旧请求会被新请求顶掉，不再长期卡在“获取中/排队刷新”
小时缓存刷新改成真正的增量补数，不再误判成从 2021年 整段全量回补
版本显示链统一到 v0.3.84

### v0.3.84 - v0.3.83

- 修复 `Deribit 波动率指数` 在切换币种/周期、点击 `立即刷新` 后，状态长期停留在“获取中/排队刷新”的问题
- 切换选择时，新请求会直接顶掉旧请求结果，旧请求返回后会被忽略，不再把界面卡在排队状态
- 修复 `Deribit 波动率指数` 小时缓存刷新策略，首次建底仓后改为真正的增量补数，不再因为误判条件每次从 `2021年` 整段全量回补
- `Deribit 波动率指数` 标题、菜单和包内兜底版本统一到 `v0.3.84`

### v0.3.82

- `Deribit 波动率指数` 切换币种 / 周期时，会先立即使用缓存出图，再强制后台刷新最新数据
- 若上一次请求仍在处理中，新选择会排队，旧请求结束后会自动刷新为最新选择
- `立即刷新` 现在会抢占旧的等待链路；若刷新完成但没有新增K线，会明确提示 `当前暂无新增K线`

### v0.3.81

- 修复回测窗口与交易明细相关的异常链路，恢复 `交易明细` 表格正常显示
- 回测窗口、参数摘要与详情区的中文文案恢复正常
- 版本显示链继续统一，避免主窗口与实际代码版本不一致

### v0.3.80

- `Deribit 波动率指数` 自动刷新改为按本机整点后触发小时增量请求
- 不再根据中间时段的最后一根K线反复请求，减少 `HTTP 429 / Too Many Requests`

### v0.3.79

- `Deribit 波动率指数` 日志新增 `最后一根K线` 时间
- 自动刷新改成按“最后一根K线的下一根边界”排程

### v0.3.78 - v0.3.72

- 回测报告支持在结束时单独列出 `期末未平仓`
- 回测交易明细新增：
  - `止损值`
  - `ATR值`
  - `开仓数量`
- 回测默认手续费更新为：
  - `Maker 0.015%`
  - `Taker 0.036%`
- 回测放大图十字光标信息框新增 `ATR(10)`
- `EMA 动态委托` 取消大周期 EMA 过滤，版本显示链同步统一
- `Deribit 波动率指数` 页面对打开后自动加载、全量 `1小时` 底仓缓存、快速切换和自动刷新进一步收口

### v0.3.66

- 主界面持仓刷新超时时，会继续显示上一份成功持仓缓存，不再清空界面
- 主界面和 Deribit 波动率指数页面的英文超时提示统一改成中文
- Deribit 波动率指数在已有图表时，网络超时会改成状态栏软提示，不再强制弹窗中断

### v0.3.71

- 修复主窗口标题和系统菜单版本显示错误的问题
- 版本号优先读取 `pyproject.toml`
- 打包运行时若无法读取 `pyproject.toml`，会回退到包内 `okx_quant.__version__` 作为兜底版本

### v0.3.70 - v0.3.69

- 清理 `Deribit 波动率指数` 页面残留的 `显示历史明细` 旧功能
- 界面不再直接显示历史明细区域，页面更简洁

### v0.3.68

- `期权策略计算器` 图表大窗里的 `波动率K线`
- 以及 `叠加对比` 下图
- 已切换为使用 `Deribit 波动率指数` 本地缓存，而不是历史波动率估算

### v0.3.65

- `Deribit 波动率指数` 改为以 `2021年` 起的全量 `1小时` 数据做本地底仓缓存
- 默认只显示最近 `300` 根，缩小视图或向左拖动时可继续查看更早历史
- `4小时 / 1天` 切换直接基于本地 `1小时` 缓存聚合，切换明显加快
- `历史K线明细` 改为默认隐藏，只有勾选时才填充

### v0.3.64

- `Deribit 波动率指数` 的 `1小时` 周期 `K线数量` 上限提升到 `30000`
- 参数错误提示改为按当前周期动态显示真实上限

### v0.3.63

- `Deribit 波动率指数` 会记住上次选择的 `日线对齐`
- 下次重新打开页面时会自动恢复

### v0.3.62

- `Deribit 波动率指数` 切换速度优化
- 改为以 `1小时` K 线做底层本地缓存
- 首次打开先读本地缓存，再后台补最新小时数据
- 切换到 `4小时 / 1天` 时，直接基于本地 `1小时` 数据聚合生成

### v0.3.61

- 修复 `Deribit 波动率指数` 页面 `_resolution_label_for_value` 缺失导致的软件打不开问题

### v0.3.60

- `Deribit 波动率指数` 十字光标黑框加大、加深，更便于读数

### v0.3.59

- `Deribit 波动率指数` 打开页面自动加载历史K线
- 切换币种、周期、K线数量后自动切图
- 默认隐藏 `历史K线明细`
- 主界面“我的策略”区域同步收紧空白

### v0.3.58

- 修复 `Deribit 波动率指数` 双图K线在极值附近显示不全的问题
- 为上下价格区间增加边距，最高价和最低价K线不再贴边

### v0.3.57

- `Deribit 波动率指数` 页面默认 `K线数量` 调整为 `300`

### v0.3.56

- `Deribit 波动率指数` 的 `1天` 周期改为先按 `4小时` 对齐再聚合成日线
- 新增两种日线对齐模式：
  - `北京时间凌晨12点`
  - `北京时间早上8点`
- 去掉 `12小时` 与 `1秒` 周期选项
- 十字光标说明框固定到远离十字区的位置，避免挡住读图

### v0.3.55

- `Deribit 波动率指数` 日线支持本地聚合与对齐收线
- 解决因不同收盘时间造成的联动共同K线缺失问题

### v0.3.52

- Deribit 波动率指数窗口升级为双图联动视图：上图波动率K线、下图同币种同周期现货K线
- 支持 `平均K线` 切换，双图会同步切换为平均K线口径
- 双图十字光标、缩放和拖动保持联动
- 十字光标提示框缩小并减淡，降低对K线观察的遮挡

### v0.3.49

- 修复 `历史仓位` 中 `BTC-USD-260626 / BTC-USDT-260626` 这类历史交割合约被错误识别成 `SPOT`，导致平仓数量显示成原始张数的问题
- 历史页现在会正确识别历史交割合约并按交割合约口径换算成币数

### v0.3.48

- 修复 `历史成交` 中交割补单数量换算错误的问题
- 对来自账单的 `FUTURES delivery` 记录，优先使用账单 `fillSz`，否则按账单面值与成交价换算成币数

### v0.3.47 - v0.3.46

- 修复现货、交割历史页的数量单位显示问题
- `历史成交 / 历史仓位` 支持全品种统一筛选：
  - `类型`
  - `方向 / 保证金模式`
  - `标的`
  - `到期前缀`
  - `搜索`

### v0.3.45 - v0.3.32

- 新增 `期权展期建议` 独立窗口
- 支持针对卖方单腿期权扫描展期/移仓候选
- 支持一键送入 `期权策略计算器`

