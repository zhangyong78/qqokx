---
description: 用户发「123」时执行发版流水线（版本号、README 日志、打包、提交、推送）
alwaysApply: true
---

# 发版口令：123

当用户在对话里**主要内容为 `123`**（或明确写「按 123 发版」）时，**默认执行**下面整套流程，无需再复述需求。

## 执行顺序

1. **版本号**（两处必须一致）  
   - `pyproject.toml` → `[project].version`  
   - `okx_quant/__init__.py` → `__version__`  
   - 在现有版本上按 **patch** 递增（如 `0.5.08` → `0.5.09`），除非用户另有说明。

2. **升级说明 / 文档**  
   - `README.md`：文首「当前版本」、`## 11. 更新日志` 里的「当前版本」、以及**新增**一节 `### vX.Y.ZZ`（用简洁中文列本版用户可见改动；若用户未写要点，根据 `git diff` / 近期提交摘要归纳）。  
   - `软件开发指南.md`：文首「当前版本」与「## 0. 里程碑版本」段落与新版对齐。

3. **打包**  
   - 在项目根执行：`python scripts/build_server_package.py`  
   - 产物在 `dist/`（目录被 `.gitignore` 忽略，**不要**把 zip 提交进仓库）。

4. **测试（发版前尽量跑）**  
   - 至少跑与本次改动相关的 `pytest`；若有失败，先修再提交，或向用户说明阻塞原因。

5. **Git**  
   - `git add` 应进版本库的文件（不含 `dist/`）→ `commit`（信息里带版本号）→ `git push` 到**当前分支**的 `origin`，除非用户指定其他分支。

## 例外

用户若同时说明「不要升版本」「只打包不推送」「不要动某某文件」等，以**用户补充为准**，覆盖上述默认。
